# 2026-07-11 工作记录

## 会话续接：环境状态核查
- Docker：haifeng-postgres(5432) 与 haifeng-redis(6379) 均 Up 且 healthy，compose 环境正常。
- 管理端应用（admin，原 8081）：当前未运行，8081 端口 CLOSED。
  - 系统中唯一 java 进程 PID 23792 是 IntelliJ 的 JPS 编译守护进程（org.jetbrains.jps.cmdline.BuildMain），并非 Spring Boot 应用。
- 结论：上一会话的 NoClassDefFoundError（UniversityGuideListVO$Builder）对应的陈旧 JVM 已不存在，错误不再存活；重新启动 admin 应用即可从磁盘完整加载类，不会再复现。
- 防复发规则（重申）：应用 JVM 存活期间，禁止执行 `mvn clean` / `mvnw clean` / IDE Rebuild / Build Project；项目无 devtools，无安全热更新，磁盘改写与运行时类加载竞争会导致 $Builder 缺失。
