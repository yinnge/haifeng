# 总的项目需求

## 数据库表：

### flyway已实现的数据库表：
地址：Project-HaiFeng\haifeng-admin\src\main\resources\db\migration

#### 原始表
位置：D:\exeProject\ideaProjects\Project-HaiFeng\haifeng-admin\src\main\resources\db\migration\C-table\table1.md

#### 需要用到之前的表
````
1. system_settings（系统设置）

CREATE TABLE system_settings (
id                BIGSERIAL PRIMARY KEY,
site_name         VARCHAR(50),
site_url          VARCHAR(100),
site_icp          VARCHAR(100),
site_description  TEXT,
api_number        INTEGER DEFAULT 3,
pro_price         INTEGER DEFAULT 199,
vip_price         INTEGER DEFAULT 599,
pro_commission_rate   SMALLINT DEFAULT 10 CHECK (pro_commission_rate >= 0 AND pro_commission_rate <= 100),
vip_commission_rate   SMALLINT DEFAULT 15 CHECK (vip_commission_rate >= 0 AND vip_commission_rate <= 100),
seo_title         VARCHAR(200),
seo_keywords      VARCHAR(100),
seo_description   TEXT,
contact_url       JSONB DEFAULT '{}',
basic_message     JSONB DEFAULT '{}',
created_at        TIMESTAMPTZ DEFAULT NOW(),
updated_at        TIMESTAMPTZ DEFAULT NOW()
);

COMMENT ON TABLE system_settings IS '系统设置表（单例）';
COMMENT ON COLUMN system_settings.pro_commission_rate IS 'Pro会员提成比例（0-100），代表0%到100%';
COMMENT ON COLUMN system_settings.vip_commission_rate IS 'VIP会员提成比例（0-100），代表0%到100%';
COMMENT ON COLUMN system_settings.contact_url IS 'JSON格式：{wechat, weibo, zhihu, douyin, bilibili}';
COMMENT ON COLUMN system_settings.basic_message IS 'JSON格式：{address, phone, email, consultationTime}';
````

2. 用户表 (t_member)
````
CREATE TABLE IF NOT EXISTS t_member (
    id                          BIGSERIAL PRIMARY KEY,
    username                    VARCHAR(50) NOT NULL UNIQUE,
    password                    VARCHAR(100) NOT NULL,
    avatar                      VARCHAR(500),
    phone                       VARCHAR(20) UNIQUE NOT NULL,
    invite_code                 VARCHAR(8) UNIQUE,
    member_type                 VARCHAR(20) DEFAULT 'normal',
    expire_at                   TIMESTAMPTZ,
    referrer_id                 BIGINT,
    referrer_username           VARCHAR(50),
    commission_balance          DECIMAL(10,2) DEFAULT 0.00,
    commission_total_earned     DECIMAL(10,2) DEFAULT 0.00,
    commission_total_paid       DECIMAL(10,2) DEFAULT 0.00,
    status                      VARCHAR(20) DEFAULT 'active',
    last_login_at               TIMESTAMPTZ,
    last_login_ip               VARCHAR(50),
    wechat_id                   VARCHAR(255),                       -- 微信号(AES加密存储)
    wechat_id_index             VARCHAR(64),                        -- 微信号盲索引(SHA-256)
    is_deleted                  BOOLEAN NOT NULL DEFAULT FALSE,
    created_at                  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at                  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    CONSTRAINT chk_member_type CHECK (member_type IN ('normal', 'pro', 'vip')),
    CONSTRAINT chk_member_status CHECK (status IN ('active', 'disabled')),
    CONSTRAINT chk_commission_balance CHECK (commission_balance >= 0)
);

CREATE INDEX idx_member_phone ON t_member(phone) WHERE is_deleted = FALSE;
CREATE INDEX idx_member_type ON t_member(member_type) WHERE is_deleted = FALSE;
CREATE INDEX idx_member_referrer ON t_member(referrer_id) WHERE is_deleted = FALSE;
CREATE INDEX idx_member_wechat_index ON t_member(wechat_id_index) WHERE is_deleted = FALSE;


````

#### 新增的表

1. 用户资料表 (t_member_profile)


#### 实现模块

### 已实现的功能：
1. 用户权限问题
2. 以前表的所有的mapper，entity都已实现，在：D:\exeProject\ideaProjects\Project-HaiFeng\haifeng-common\src\main\java\com\haifeng\common，的下面

### 待实现需求：
首页管理[父模块]--界面管理[子模块]
用户管理[父模块]--个人中心[子模块]

**首页内容**：无任何权限都可以访问
**用户资料表(个人中心)**：必须使用户权限才可以

1. 前端动态展示数据表里的数据，只展示信息
2. 个人中心展示个人资料


### 任务1【完成数据表】
根据resources\db\migration\C-table\table1.md的数据库表，创建并完善apps_V16__user_profile__tables.sql,放到flyway的迁移下面，要注意索引，注释等规范

### 任务2-1【完成个人中心-用户资料展示 (t_member_profile)】
- 用户资料表(t_member_profile)
1. 完善：D:\exeProject\ideaProjects\Project-HaiFeng\haifeng-common\src\main\java\com\haifeng\common\entity\user\MemberProfile.java
2. 接口用户资料信息，支持查和改的字段：real_name，email，gender，school_name，province，city，major，identity，grade，education_level
限制：
1.创建省份的枚举
2.专业，城市，school_name都有对应的表的，需要实现模糊查询（默认返回 10 条，最多 20 条），只能根据我们数据库已有的表的数据来填写：D:\exeProject\ideaProjects\Project-HaiFeng\haifeng-common\src\main\java\com\haifeng\common\entity，下已经实现了
3.注意：
- 仅当 identity = 大学生 或 研究生 时，才显示/允许编辑 schoolName
- 当 identity = 高中生 或 其他 时，schoolName 字段不返回/不允许修改   
- 若用户身份从 大学生/研究生 改为 高中生/其他，schoolName 自动置空
4.新增枚举类：IdentityEnum，ProvinceEnum，GenderEnum

**用户表(t-member)**
1. wechat_id(AES加密存储)，avatar，phone，username，password
2. 点击查看微信号，需要解密展示具体的微信号，admin之前实现过的
3. 可以修改微信号

### 任务2-2【完成个人中心-用户资料别的信息展示(t-member)】

1. 需要返回任务2的数据，这些支持修改和查看
2. 下面的不支持修改，只支持查看[用户表]：
   - 邀请码
     invite_code
   - 提现金额
     commission_balance          DECIMAL(10,2)   DEFAULT 0.00,       -- 可提现余额
     commission_total_earned     DECIMAL(10,2)   DEFAULT 0.00,       -- 累计获得佣金
     commission_total_paid       DECIMAL(10,2)   DEFAULT 0.00,       -- 累计已发放佣金
   - 用户身份
     member_type

### 任务3【完成个人中心-提现功能】
1.需要字段：
- 金额
  commission_balance          DECIMAL(10,2)   DEFAULT 0.00,       -- 可提现余额
  commission_total_earned     DECIMAL(10,2)   DEFAULT 0.00,       -- 累计获得佣金
  commission_total_paid       DECIMAL(10,2)   DEFAULT 0.00,       -- 累计已发放佣金
- 邀请码
  invite_code
- 邀请人数
  联表查询邀请码，对应用户的数量
2.佣金提现
- 点击提现，先判断是否大于50或100，然后选择50或100提现（好像是前端实现）
- 可以的话生成一条提现记录表，将用户的id，phone，wechat_id，wechat_id_index，选择的50.00/100.00（对应amount）存储进去即可
3.支持添加用户的外键邀请码，只有一个，而且之前用户填写过邀请码的不允许修改或添加【前提：用户之前注册的时候没有填写邀请码】
逻辑：我们展示的是：referrer_id对应的invite_code。然后之前注册用户有的没有填写邀请码，这里肯定是空，那么可以让用户填写邀请码，然后联表查询返回对应的用户的username，phone，用户点击确认，把用户的referrer_id绑定成功，后续不可修改。

#### 说明
1. 邀请码不允许填自己
2. 邀请码不允许两个人循环
3. 当用户填写的邀请码对应的用户被禁用或删除的时候，也不允许修改，我后台做了实现，你只需知道哪怕对应用户禁用或删除，也不允许修改referred_id


### 任务4【完成-界面管理和渲染】

1. 返回site_icp，contact_url（JSONB）里面所有的字段，basic_message（JSONB）里面所有的字段
2. 不经常修改，可以缓存吗？
3. 实体类在：D:\exeProject\ideaProjects\Project-HaiFeng\haifeng-common\src\main\java\com\haifeng\common\entity\system


### 功能统一需求

1. 统一用雪花算法
2. 按子模块独立Controller，每个子模块独立Controller

### 总结
前端的一个开关，主界面不限制用户，哪怕不登录也能看到，用户个人中心和资料就要提示登录了，然后跳转登录，所以你要判断他身份，只能是：'normal', 'pro', 'vip'

