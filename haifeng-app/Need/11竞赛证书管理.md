# 总的项目需求

#### 需要用到的数据库表
````
-- ============================================
-- V9__create_certificate_competition.sql
-- 竞赛证书管理模块：证书表、竞赛表、竞赛详情表、竞赛-专业关联表
-- ============================================

-- 1. 专业主表 (t_major)
-- ===========================================================
CREATE TABLE t_major (
    id                  BIGINT          PRIMARY KEY,
    major_code          VARCHAR(20)     NOT NULL,
    major_name          VARCHAR(100)    NOT NULL,
    discipline_name     VARCHAR(100),
    major_type          VARCHAR(30)     NOT NULL,
    major_category      VARCHAR(50),
    parent_category     VARCHAR(50),
    major_tags          VARCHAR(50),
    degree_awarded      VARCHAR(50),
    employment_rate     NUMERIC(5, 2),
    salary_min          INTEGER,
    salary_max          INTEGER,
    description         TEXT,
    status              SMALLINT        NOT NULL DEFAULT 1,
    created_at          TIMESTAMPTZ     NOT NULL DEFAULT NOW(),
    updated_at          TIMESTAMPTZ     NOT NULL DEFAULT NOW(),

    -- 约束
    CONSTRAINT uk_major_code UNIQUE (major_code),
    CONSTRAINT chk_major_employment_rate CHECK (
        employment_rate IS NULL OR (employment_rate >= 0 AND employment_rate <= 100)
    ),
    CONSTRAINT chk_major_salary_range CHECK (
        (salary_min IS NULL AND salary_max IS NULL)
        OR (salary_min IS NULL)
        OR (salary_max IS NULL)
        OR (salary_min >= 0 AND salary_max >= 0 AND salary_min <= salary_max)
    )
);

-- ===========================================================
-- 1. 证书表 (t_certificate)
-- 描述：各类职业资格证书、等级考试证书信息
-- ===========================================================
CREATE TABLE t_certificate (
    id                      BIGINT          PRIMARY KEY,
    cert_name               VARCHAR(150)    NOT NULL,
    category                VARCHAR(50),
    cert_level              VARCHAR(50),
    applicable_major        VARCHAR(200),
    registration_time       VARCHAR(100),
    exam_time               VARCHAR(100),
    exam_fee                INTEGER,
    cert_intro              TEXT,
    exam_requirements       TEXT[]          DEFAULT '{}',
    exam_arrangement        TEXT,
    official_website        VARCHAR(500),
    `is_deleted`              BOOLEAN         NOT NULL DEFAULT FALSE,
    created_at              TIMESTAMPTZ     NOT NULL DEFAULT NOW(),
    updated_at              TIMESTAMPTZ     NOT NULL DEFAULT NOW(),

    -- 约束
    CONSTRAINT uk_cert_name UNIQUE (cert_name),
    CONSTRAINT chk_cert_exam_fee CHECK (exam_fee IS NULL OR exam_fee >= 0)
);

-- 索引
CREATE INDEX idx_cert_category ON t_certificate (category) WHERE is_deleted = FALSE;
CREATE INDEX idx_cert_level ON t_certificate (cert_level) WHERE is_deleted = FALSE;
CREATE INDEX idx_cert_name_search ON t_certificate USING btree (cert_name varchar_pattern_ops) WHERE is_deleted = FALSE;
CREATE INDEX idx_cert_major ON t_certificate USING btree (applicable_major varchar_pattern_ops) WHERE is_deleted = FALSE;
CREATE INDEX idx_cert_requirements ON t_certificate USING gin (exam_requirements) WHERE is_deleted = FALSE;

-- 触发器
CREATE TRIGGER trg_certificate_updated_at
    BEFORE UPDATE ON t_certificate
    FOR EACH ROW
    EXECUTE FUNCTION fn_update_timestamp();



-- ===========================================================
-- 2. 竞赛表 (t_competition)
-- 描述：各类学科竞赛、创新创业大赛基本信息
-- ===========================================================
CREATE TABLE t_competition (
    id                      BIGINT          PRIMARY KEY,
    comp_name               VARCHAR(200)    NOT NULL,
    comp_level              VARCHAR(50),
    registration_time       VARCHAR(100),
    is_deleted              BOOLEAN         NOT NULL DEFAULT FALSE,
    created_at              TIMESTAMPTZ     NOT NULL DEFAULT NOW(),
    updated_at              TIMESTAMPTZ     NOT NULL DEFAULT NOW(),

    -- 约束
    CONSTRAINT uk_comp_name UNIQUE (comp_name)
);

-- 索引
CREATE INDEX idx_comp_level ON t_competition (comp_level) WHERE is_deleted = FALSE;
CREATE INDEX idx_comp_name_search ON t_competition USING btree (comp_name varchar_pattern_ops) WHERE is_deleted = FALSE;

-- 触发器
CREATE TRIGGER trg_competition_updated_at
    BEFORE UPDATE ON t_competition
    FOR EACH ROW
    EXECUTE FUNCTION fn_update_timestamp();



-- ===========================================================
-- 3. 竞赛详情表 (t_competition_detail) - 与 t_competition 1:1
-- 关联关系说明（不使用外键约束，性能考虑）：
--   competition_id -> t_competition.id
-- ===========================================================
CREATE TABLE t_competition_detail (
    id                      BIGINT          PRIMARY KEY,
    competition_id          BIGINT          NOT NULL,
    basic_info              JSONB           DEFAULT '{}'::JSONB,
    awards                  TEXT[]          DEFAULT '{}',
    background              TEXT,
    purposes                TEXT[]          DEFAULT '{}',
    competition_rules       JSONB           DEFAULT '[]'::JSONB,
    scoring_criteria        TEXT[]          DEFAULT '{}',
    notices                 TEXT[]          DEFAULT '{}',
    process_guide           JSONB           DEFAULT '[]'::JSONB,
    awards_display          JSONB           DEFAULT '[]'::JSONB,
    is_deleted              BOOLEAN         NOT NULL DEFAULT FALSE,
    created_at              TIMESTAMPTZ     NOT NULL DEFAULT NOW(),
    updated_at              TIMESTAMPTZ     NOT NULL DEFAULT NOW(),

    -- 约束
    CONSTRAINT uk_competition_detail_competition_id UNIQUE (competition_id)
);

-- 索引（competition_id 已通过 UNIQUE 约束自动创建索引）
CREATE INDEX idx_comp_detail_awards ON t_competition_detail USING gin (awards) WHERE is_deleted = FALSE;
CREATE INDEX idx_comp_detail_basic_info ON t_competition_detail USING gin (basic_info) WHERE is_deleted = FALSE;

-- 触发器
CREATE TRIGGER trg_competition_detail_updated_at
    BEFORE UPDATE ON t_competition_detail
    FOR EACH ROW
    EXECUTE FUNCTION fn_update_timestamp();



-- ===========================================================
-- 4. 竞赛-专业关联表 (t_competition_major) - 多对多
-- 关联关系说明（不使用外键约束，性能考虑）：
--   competition_id -> t_competition.id
--   major_id -> t_major.id
-- ===========================================================
CREATE TABLE t_competition_major (
    id                      BIGINT          PRIMARY KEY,
    competition_id          BIGINT          NOT NULL,
    major_id                BIGINT          NOT NULL,
    major_name              VARCHAR(100)    NOT NULL,
    competition_name        VARCHAR(200)    NOT NULL,
    created_at              TIMESTAMPTZ     NOT NULL DEFAULT NOW(),

    -- 约束
    CONSTRAINT uk_comp_major UNIQUE (competition_id, major_id)
);

-- 索引
-- 竞赛详情页：该竞赛适合哪些专业
CREATE INDEX idx_cm_competition ON t_competition_major (competition_id);
-- 专业详情页：该专业能参加哪些竞赛（反向查询）
CREATE INDEX idx_cm_major ON t_competition_major (major_id);



````

#### 实现模块

### 已实现的功能：
1. 用户权限问题，已实现注解了：D:\0code\haifeng\backend\haifeng\haifeng-common\src\main\java\com\haifeng\common\annotation
2. 以前表的所有的mapper，entity都已实现，在：D:\exeProject\ideaProjects\Project-HaiFeng\haifeng-common\src\main\java\com\haifeng\common，的下面

### 待实现需求：
竞赛证书管理[父模块]--科研竞赛列表[子模块]--职业技能证书列表[子模块]

1. 前端动态展示数据表里的数据，只展示信息

### 任务1【完成对证书表的查询】
接口1：分页展示证书表的id,cert_name,category,cert_level,applicable_major,registration_time,exam_time,exam_fee,cert_intro。
      支持根据category精准查询,支持根据cert_name模糊查询
接口2：查询证书表的id,cert_name,category,cert_level,applicable_major,registration_time,exam_time,exam_fee,cert_intro，exam_requirements，exam_arrangement，official_website
注意：
1. 接口1不需要登录
2. 接口2至少需要登录才可以访问


### 任务2【完成对竞赛表的查询】
接口1竞赛表:分页展示t_competition表的id,comp_name,comp_level,registration_time
接口2竞赛详情：根据分页查出来的的id关联t_competition_detail的competition_id查询t_competition_detail表的basic_info,awards,background,purposes,competition_rules,scoring_criteria,notices,process_guide,awards_display
接口3：根据分页查出来的的id关联t_competition_major的competition_id查询major_id,major_name
注意：
1. 接口1不需要登录
2. 接口2至少需要登录（normal）才可以访问
3. 接口3至少需要pro才可以访问


### 任务3【完成对专业表的查询】
接口1：根据分页查询出来的具体的专业id联表t_competition_major查询出competition_id和competition_name

#### 注意
##### 地址
D:\0code\haifeng\backend\haifeng\haifeng-app\src\main\java\com\haifeng\app\controller\major\MajorController.java

/** 任务1接口1：专业列表（公开） */
@GetMapping("/list")
public R<IPage<MajorListVO>> list(@Valid MajorListQueryDTO dto) {
return R.ok(majorService.page(dto));
}

1. 分页查询专业id已经实现
2. 接口一至少需要pro用户才可以访问


### 注意事项
1. JSONB字段的在common实现过，不确定或找不到的可以问我
2. 所有的身份都已实现注解，在：D:\0code\haifeng\backend\haifeng\haifeng-common\src\main\java\com\haifeng\common\annotation
3. 表中JSONB字段直接返回，由前端渲染。
4. 不要使用git命令提交，最后修改的统一提交，遇到冲突的需要给我判决，一般都不会遇到。
