# 总的项目需求

#### 需要用到的数据库表
````
-- ============================================
-- V6__create_universities_details.sql
-- 院校次表：实验室、院系、学科评估
-- ============================================

-- 院校主表
CREATE TABLE t_universities (
    id                  BIGINT        PRIMARY KEY,
    name                VARCHAR(50)   NOT NULL,
    name_en             VARCHAR(50)   NOT NULL,
    province_name       VARCHAR(50)   NOT NULL,
    city_name           VARCHAR(50)   NOT NULL,
    region              VARCHAR(50)   NOT NULL,
    category            VARCHAR(50)   NOT NULL,
    major_count         INTEGER       DEFAULT 0,
    education_level     VARCHAR(50),
    nature              VARCHAR(50),
    recommendation_rate DECIMAL(5,2),
    recommendation_year INTEGER,
    has_doctorate       BOOLEAN       DEFAULT false,
    has_master          BOOLEAN       DEFAULT false,
    department          VARCHAR(100),
    tags                TEXT[],
    famous_union        VARCHAR(50),
    image_url           VARCHAR(500),
    introduction        TEXT,
    sort_order          INTEGER       DEFAULT 0,
    status              SMALLINT      DEFAULT 1 NOT NULL,
    created_at          TIMESTAMPTZ   DEFAULT CURRENT_TIMESTAMP NOT NULL,
    updated_at          TIMESTAMPTZ   DEFAULT CURRENT_TIMESTAMP NOT NULL
);



-- 1. 实验室表 (laboratories)
CREATE TABLE laboratories (
    id                    BIGINT        PRIMARY KEY,
    university_id         BIGINT        NOT NULL,
    university_name       VARCHAR(50)   NOT NULL,
    name                  VARCHAR(200)  NOT NULL,
    lab_type              VARCHAR(100)  NOT NULL,
    established_year      VARCHAR(20),
    region                VARCHAR(100),
    department            VARCHAR(100),
    director              VARCHAR(50),
    staff_count           VARCHAR(50),
    student_count         VARCHAR(50),
    email                 VARCHAR(200),
    phone                 VARCHAR(50),
    introduction          TEXT,
    research_description  TEXT,
    lab_space             TEXT,
    open_topics           TEXT,
    cooperation           TEXT,
    visiting_scholars     TEXT,
    research_fields       TEXT[],
    statistics            JSONB         DEFAULT '[]'::JSONB,
    major_equipment       TEXT[],
    core_team             JSONB         DEFAULT '[]'::JSONB,
    sort_order            INTEGER       DEFAULT 0,
    status                SMALLINT      DEFAULT 1 NOT NULL,
    created_at            TIMESTAMPTZ   DEFAULT CURRENT_TIMESTAMP,
    updated_at            TIMESTAMPTZ   DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_labs_university_id ON laboratories(university_id);
CREATE INDEX idx_labs_lab_type ON laboratories(lab_type);
CREATE INDEX idx_labs_region ON laboratories(region);
CREATE INDEX idx_labs_status ON laboratories(status);
CREATE INDEX idx_labs_name ON laboratories(name);



-- 2. 院系主表 (t_department)
CREATE TABLE t_department (
    id                    BIGINT        PRIMARY KEY,
    university_id         BIGINT        NOT NULL,
    university_name       VARCHAR(50)   NOT NULL,
    department_name       VARCHAR(100)  NOT NULL,
    department_type       VARCHAR(100)  NOT NULL,
    page_title            VARCHAR(200),
    tags                  TEXT[],
    sort_order            INTEGER       DEFAULT 0,
    status                SMALLINT      DEFAULT 1 NOT NULL,
    created_at            TIMESTAMPTZ   DEFAULT CURRENT_TIMESTAMP,
    updated_at            TIMESTAMPTZ   DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_dept_university_id ON t_department(university_id);
CREATE INDEX idx_dept_status ON t_department(status);
CREATE INDEX idx_dept_name ON t_department(department_name);
CREATE INDEX idx_dept_type ON t_department(department_type);



-- 3. 院系分析报告表 (department_reports)
CREATE TABLE department_reports (
    id                    BIGINT        PRIMARY KEY,
    department_id         BIGINT        NOT NULL UNIQUE,
    subtitle              VARCHAR(200),
    overview              JSONB         DEFAULT '{}'::JSONB,
    subjects_detail       JSONB         DEFAULT '[]'::JSONB,
    postgraduate          JSONB         DEFAULT '{}'::JSONB,
    city_salary           JSONB         DEFAULT '[]'::JSONB,
    salary                JSONB         DEFAULT '[]'::JSONB,
    career                JSONB         DEFAULT '[]'::JSONB,
    trends                JSONB         DEFAULT '{}'::JSONB,
    prospects             JSONB         DEFAULT '{}'::JSONB,
    disclaimer            JSONB         DEFAULT '{}'::JSONB,
    major_compose         JSONB         DEFAULT '[]'::JSONB,
    sort_order            INTEGER       DEFAULT 0,
    status                SMALLINT      DEFAULT 1 NOT NULL,
    created_at            TIMESTAMPTZ   DEFAULT CURRENT_TIMESTAMP,
    updated_at            TIMESTAMPTZ   DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_dept_reports_department_id ON department_reports(department_id);
CREATE INDEX idx_dept_reports_status ON department_reports(status);



-- 4. 学科评估表 (t_subject_evaluation)
CREATE TABLE t_subject_evaluation (
    id                    BIGINT        PRIMARY KEY,
    university_id         BIGINT        NOT NULL,
    university_name       VARCHAR(100)  NOT NULL,
    discipline_code       VARCHAR(20)   NOT NULL,
    discipline_name       VARCHAR(100)  NOT NULL,
    evaluation_round      VARCHAR(20)   DEFAULT '第四轮',
    evaluation_grade      VARCHAR(5)    NOT NULL,
    sort_order            INTEGER       DEFAULT 0,
    status                SMALLINT      DEFAULT 1 NOT NULL,
    created_at            TIMESTAMPTZ   DEFAULT CURRENT_TIMESTAMP,
    updated_at            TIMESTAMPTZ   DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT chk_eval_grade CHECK (
        evaluation_grade IN ('A+', 'A', 'A-', 'B+', 'B', 'B-', 'C+', 'C', 'C-')
    )
);

CREATE INDEX idx_se_university ON t_subject_evaluation(university_id);
CREATE INDEX idx_se_discipline ON t_subject_evaluation(discipline_name, evaluation_grade);
CREATE INDEX idx_se_grade ON t_subject_evaluation(evaluation_grade);
CREATE INDEX idx_se_status ON t_subject_evaluation(status);


````

#### 实现模块

### 已实现的功能：
1. 用户权限问题，已实现注解了：D:\0code\haifeng\backend\haifeng\haifeng-common\src\main\java\com\haifeng\common\annotation
2. 以前表的所有的mapper，entity都已实现，在：D:\exeProject\ideaProjects\Project-HaiFeng\haifeng-common\src\main\java\com\haifeng\common，的下面

### 待实现需求：
院校管理[父模块]--实验室列表[子模块]--学科评估[子模块]--院系[子模块]

**实验室列表**：需要登录才可以访问
**学科评估**：需要登录才可以访问
**院系**：需要登录才可以访问

1. 前端动态展示数据表里的数据，只展示信息

注意：需要登录
### 任务1【完成对实验室列表查询】
接口1实验室：根据t_universities表的id关联laboratories表的university_id查询laboratories表的name,lab_type
接口2实验室详情：根据laboratories表的name查询出university_name,lab_type,established_year,region,department,director,staff_count,student_count,email,phone,introduction,research_description,lab_space,open_topics,cooperation,visiting_scholars,research_fields,statistics,major_equipment,core_team

注意：需要登录权限
### 任务2【完成对院系列表查询】
接口1院系：根据t_universities表的id关联t_department表的university_id查询department_name,department_type
接口2院系分析报告：根据department_reports表的department_id关联t_department表的id查询department_reports表的subtitle,overview,subjects_detail,postgraduate,city_salary,salary,career,trends,prospects,disclaimer,major_compose



注意：需要登录权限
### 任务3【完成对学科评估表的查询】
接口1：根据t_universities表的id关联t_subject_evaluation表的university_id查询evaluation_round,evaluation_grade，discipline_code，discipline_name，统计数据（9个）
实现2：CONSTRAINT chk_eval_grade CHECK (
evaluation_grade IN ('A+', 'A', 'A-', 'B+', 'B', 'B-', 'C+', 'C', 'C-')
)
需要你对evaluation_grade的每一个值对应的discipline_code字段，统计出个数并返回count并返回展示，与evaluation_grade对应。
实例：A+ 数量：37门


### 注意事项
1. JSONB字段的在common实现过，不确定或找不到的可以问我
2. 所有的身份都已实现注解，在：D:\0code\haifeng\backend\haifeng\haifeng-common\src\main\java\com\haifeng\common\annotation
3. 表中JSONB字段直接返回，由前端渲染。
4. 不要使用git命令提交，最后修改的统一提交。
