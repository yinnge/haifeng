# 总的项目需求

## 数据库表：

### flyway已实现的数据库表：
地址：Project-HaiFeng\haifeng-admin\src\main\resources\db\migration
#### V1__create_admin_tables.sql
1. admin_logs
2. sys_role
3. sys_module
4. sys_role_module
5. sys_admin
6. t_member
7. 会员订单表 (member_orders)
8. 推荐佣金表 (t_referral_commission)
9. 消息通知表 (t_member_notification)
10. 系统设置表 (system_settings)
11. 公告表 (t_announcements)
12. 规划师表 (t_planners)
13. 培训机构表 (t_institutions)

### 本次新增的表：
1. 院校表 (universities)
2. 院校详情表 (universities_detail)
3. 校园图册表（t_campus_gallery）
4. 院校适应指南表 (university_guides)


#### 原始表
位置：Project-HaiFeng\haifeng-admin\src\main\resources\db\migration\table\DataTable5.md

### 需求：
后台管理： 院校管理[父模块]--院校列表[子模块]--校园图册[子模块]--院校适应指南[子模块]--院系详情[子模块]--实验室列表[子模块]--学科评估[子模块]

### 本次需要实现的：
院校列表[子模块]--校园图册[子模块]--院校适应指南[子模块]

**实现需求：**
**仅限管理员身份**
1. 分页展示院校列表，右边三个按钮：查看详情、修改、删除
2. 查看详情和修改，实现用 Tab 标签页 或 卡片锚点 把字段区分开：
- Tab 1：基础信息（对应 院校表 的字段）
- Tab 2：详细介绍（对应 院校详情表 的字段）
3. 实现一个功能：支持导入两张xlsx（一张是院校表，一张是院校详情表），实现了批量导入数据的功能，后续会详细介绍
4. 实现分页查询院校图册表(t_campus_gallery),支持增删改查,右边有删除，修改按钮。并实现一个导入xlsx批量导入数据库的功能
5. 实现院校适应指南表 (university_guides)的分页列表，支持增删改查,右边有删除，修改按钮。并实现一个导入xlsx批量导入数据库的功能
6. 需要引入**Alibaba EasyExcel**的依赖


### 任务1【完成数据表】
根据db\migration\table\DataTable5.md的数据库表，创建并完善V5__create_universitys_tables.sql,放到flyway的迁移下面

### 任务2【完成院校列表(universities)和院校详情表(universities_detail)】
1. 列表展示：name,province_name,city_name，region，category,major_count,education_level,nature
2. 点击详情，展示院校表和院校详情表的所有字段，注意有一个JSONB字段，在DataTable5.md写清楚了
3. 实现xlsx批量导入功能，两个xlsx

字段说明：
| 字段名          | 类型           | 是否必填 | 说明                 | 示例值                              |
| ------------ | ------------ | ---- | ------------------ | -------------------------------- |
| **院校名称**     | 文本           | ✅ 必填 | 院校官方全称             | 清华大学                             |
| **院校名称英文**   | 文本           | ✅ 必填 | 院校英文名称             | Tsinghua University              |
| **省份**       | 文本           | ✅ 必填 | 所在省份               | 北京                               |
| **城市**       | 文本           | ✅ 必填 | 所在城市               | 北京                               |
| **所属地区**     | 文本           | ✅ 必填 | 地区（通常与省份相同）        | 北京                               |
| **院校类别**     | 文本           | ✅ 必填 | 院校类型（见下方可选值）       | 综合                               |
| **专业数量**     | 整数           | 可选   | 开设专业总数             | 90                               |
| **办学层次**     | 文本           | 可选   | 本科/专科/本专兼招（见下方可选值） | 本科                               |
| **院校性质**     | 文本           | 可选   | 公办/民办/中外合作（见下方可选值） | 公办                               |
| **是否有博士点**   | 布尔           | 可选   | TRUE=有 / FALSE=无   | TRUE                             |
| **隶属部门**     | 文本           | 可选   | 主管部门               | 教育部                              |
| **院校标签**     | 文本           | 可选   | 标签列表（请用逗号分隔）       | 985,211,双一流                      |
| **院校图片URL**  | 文本           | 可选   | 院校代表图片链接           | https://example.com/tsinghua.jpg |
| **院校简介**     | 文本           | 可选   | 院校介绍文字             | 清华大学是中国著名高等学府...                 |
| **知名联盟**     | 文本           | 可选   | 院校联盟               | C9联盟，E9联盟，G7联盟，Z14联盟             |
| **是否有硕士点**   | 布尔           | 可选   | TRUE=有 / FALSE=无   | TRUE                             |
| **推免率（百分比**） | DECIMAL(5,2) | 可选   | 五位数，小数点后两位         |                                  |
| **推免年份**     | INT          | 可选   | 2024               |                                  |
....还有：是否有博士点，是否有硕士点，隶属部门，院校层次，知名联盟，院校图片URL，院校简介

4. 第二个xlsx：院校详情表说明

| 字段名             | 说明                       | 类型/约束                 |
| --------------- | ------------------------ | --------------------- |
| 院校名称            | 院校名称(外键,填写院校表中的院校名称,一对一) | VARCHAR(100) NOT NULL |
| 学校地址            | 学校地址,如:北京市海淀区清华园1号       | VARCHAR(200)          |
| 招生电话            | 招生电话,如:010-62770334      | VARCHAR(50)           |
| 官方网站            | 官方网站URL                  | VARCHAR(500)          |
| 本科批历史组          | 本科批历史组分数线                | INTEGER               |
| 本科批物理组          | 本科批物理组分数线                | INTEGER               |
| 轮播图片URL(请用逗号分隔) | 轮播图片URL列表,多个用英文逗号分隔      | TEXT[]                |
| 院校详细介绍          | 院校详细介绍                   | TEXT                  |

**注意jsonb拆分字段跟在表头的后面（拆分rankings字段）：**
| 软科排名       | 软科中国大学排名               | INTEGER               |
| ---------- | ---------------------- | --------------------- |
| 校友会排名      | 校友会中国大学排名              | INTEGER               |
| 武书连排名      | 武书连中国大学排名              | INTEGER               |
| QS排名       | QS世界大学排名               | INTEGER               |
| U.S.NEWS排名 | U.S.NEWS世界大学排名         | INTEGER               |



### 任务3【完成校园图册表（t_campus_gallery）】

1. 展示列表：展示大学名，图片类型，排序权重。支持新增，修改，删除
2. 实现一个xlsx文件上传功能，上传成功后，将数据导入数据库
字段有：university_name，image_type，image_url，sort_order

注意：这个是一对多的，允许大学名重复

### 任务4【完成院校适应指南表（university_guides）】
1. 列表展示：大学名，自定义标签【text】（逗号分隔展示），remark，status
2. 展示详情：显示所有字段，包括jsonb字段，可以借鉴tab的思想，就把他当成一个外键的表
3. 新增支持xlsx导入功能，导入所有字段

**jsonb字段：**

1. 班级与宿舍社交说明（ **对应JSONB字段**：class_dorm_social）
   | 字段名            | 说明                       | 类型/约束                 |
   | -------------- | ------------------------ | --------------------- |
   | 院校名称           | 院校名称（外键，填写院校适应指南表中的院校名称） | VARCHAR(100) NOT NULL |
   | 班级管理方式(请用逗号分隔) | 班级管理方式，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
   | 宿舍社交建议(请用逗号分隔) | 宿舍社交建议，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
2. 奖助勤贷与权益保障说明 （**对应JSONB字段**：financial_aid）
   | 字段名            | 说明                       | 类型/约束                 |
   | -------------- | ------------------------ | --------------------- |
   | 院校名称           | 院校名称（外键，填写院校适应指南表中的院校名称） | VARCHAR(100) NOT NULL |
   | 奖助学金政策(请用逗号分隔) | 奖助学金政策，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
   | 勤工俭学岗位(请用逗号分隔) | 勤工俭学岗位，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
   | 权益申诉渠道(请用逗号分隔) | 权益申诉渠道，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
3. 生活服务说明（**对应JSONB字段**：life_services）
   | 字段名            | 说明                       | 类型/约束                 |
   | -------------- | ------------------------ | --------------------- |
   | 院校名称           | 院校名称（外键，填写院校适应指南表中的院校名称） | VARCHAR(100) NOT NULL |
   | 校园生活服务(请用逗号分隔) | 校园生活服务，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
   | 医疗资源(请用逗号分隔)   | 医疗资源介绍，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
   | 兼职实习资源(请用逗号分隔) | 兼职实习资源，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
4. 水电网与宿舍管理说明（**对应JSONB字段**：dormitory_services）
   | 字段名             | 说明                       | 类型/约束                 |
   | --------------- | ------------------------ | --------------------- |
   | 院校名称            | 院校名称（外键，填写院校适应指南表中的院校名称） | VARCHAR(100) NOT NULL |
   | 水电费缴纳方式(请用逗号分隔) | 水电费缴纳方式，多个用英文逗号分隔        | TEXT[]（逗号分隔）          |
   | 宿舍规章制度(请用逗号分隔)  | 宿舍管理规定，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
5. 校园安全与应急处理说明（**对应JSONB字段**：campus_security）
   | 字段名          | 说明                       | 类型/约束                 |
   | ------------ | ------------------------ | --------------------- |
   | 院校名称         | 院校名称（外键，填写院校适应指南表中的院校名称） | VARCHAR(100) NOT NULL |
   | 安全设施(请用逗号分隔) | 安全设施介绍，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
   | 安全规则(请用逗号分隔) | 安全规则说明，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
6. 校园活动与竞赛说明（**对应JSONB字段**：campus_events）
   | 字段名             | 说明                       | 类型/约束                 |
   | --------------- | ------------------------ | --------------------- |
   | 院校名称            | 院校名称（外键，填写院校适应指南表中的院校名称） | VARCHAR(100) NOT NULL |
   | 院校品牌活动(请用逗号分隔)  | 学校品牌活动，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
   | 学科与技能竞赛(请用逗号分隔) | 学科竞赛活动，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
7. 校园设施说明（**对应JSONB字段**：campus_facilities）
   | 字段名             | 说明                       | 类型/约束                 |
   | --------------- | ------------------------ | --------------------- |
   | 院校名称            | 院校名称（外键，填写院校适应指南表中的院校名称） | VARCHAR(100) NOT NULL |
   | 教学楼分布(请用逗号分隔)   | 教学楼分布情况，多个用英文逗号分隔        | TEXT[]（逗号分隔）          |
   | 实验楼与图书馆(请用逗号分隔) | 实验楼与图书馆信息，多个用英文逗号分隔      | TEXT[]（逗号分隔）          |
   | 宿舍区与食堂(请用逗号分隔)  | 宿舍区与食堂分布，多个用英文逗号分隔       | TEXT[]（逗号分隔）          |
   | 生活配套设施(请用逗号分隔)  | 生活配套设施，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
8. 校园通勤与校外交通说明（**对应JSONB字段**：campus_transportation）
   | 字段名            | 说明                       | 类型/约束                 |
   | -------------- | ------------------------ | --------------------- |
   | 院校名称           | 院校名称（外键，填写院校适应指南表中的院校名称） | VARCHAR(100) NOT NULL |
   | 校内通勤方式(请用逗号分隔) | 校内通勤方式，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
   | 校外交通情况(请用逗号分隔) | 校外交通情况，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
9. 学生组织与社团说明（ **对应JSONB字段**：student_organizations）
   | 字段名          | 说明                       | 类型/约束                 |
   | ------------ | ------------------------ | --------------------- |
   | 院校名称         | 院校名称（外键，填写院校适应指南表中的院校名称） | VARCHAR(100) NOT NULL |
   | 官方组织(请用逗号分隔) | 官方学生组织，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
   | 社团类型(请用逗号分隔) | 社团分类，多个用英文逗号分隔           | TEXT[]（逗号分隔）          |
10. 学习支持资源说明（**对应JSONB字段**：academic_support_resources）
    | 字段名          | 说明                       | 类型/约束                 |
    | ------------ | ------------------------ | --------------------- |
    | 院校名称         | 院校名称（外键，填写院校适应指南表中的院校名称） | VARCHAR(100) NOT NULL |
    | 师资力量(请用逗号分隔) | 师资力量介绍，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
    | 学习场所(请用逗号分隔) | 学习场所信息，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
    | 学业帮扶(请用逗号分隔) | 学业帮扶措施，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
11. 医保与心理健康说明（**对应JSONB字段**：health_services）
    | 字段名            | 说明                       | 类型/约束                 |
    | -------------- | ------------------------ | --------------------- |
    | 院校名称           | 院校名称（外键，填写院校适应指南表中的院校名称） | VARCHAR(100) NOT NULL |
    | 医保报销政策(请用逗号分隔) | 医保报销政策，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
    | 心理健康服务(请用逗号分隔) | 心理健康服务，多个用英文逗号分隔         | TEXT[]（逗号分隔）          |
12. 专业与课程核心信息说明（**对应JSONB字段**：academic_guidance）
    | 字段名              | 说明                       | 类型/约束                 |
    | ---------------- | ------------------------ | --------------------- |
    | 院校名称             | 院校名称（外键，填写院校适应指南表中的院校名称） | VARCHAR(100) NOT NULL |
    | 专业培养方案说明(请用逗号分隔) | 专业培养方案说明，多个用英文逗号分隔       | TEXT[]（逗号分隔）          |
    | 选课系统说明(请用逗号分隔)   | 选课系统使用说明，多个用英文逗号分隔       | TEXT[]（逗号分隔）          |
13. 转专业限制说明（**对应JSONB字段**：major_transfer_constriction）
    | 字段名    | 说明                       | 类型/约束                 |
    | ------ | ------------------------ | --------------------- |
    | 院校名称   | 院校名称（外键，填写院校适应指南表中的院校名称） | VARCHAR(100) NOT NULL |
    | 限制类型   | 限制类型描述                   | VARCHAR(20)           |
    | 具体限制说明 | 具体限制说明内容                 | VARCHAR(50)           |
14. 转专业原则说明（major_transfer_guidelines）
    | 字段名             | 说明                       | 类型/约束                 |
    | --------------- | ------------------------ | --------------------- |
    | 院校名称            | 院校名称（外键，填写院校适应指南表中的院校名称） | VARCHAR(100) NOT NULL |
    | 基本申请条件(请用逗号分隔)  | 转专业基本条件，多个用英文逗号分隔        | TEXT[]（逗号分隔）          |
    | 申请时间与流程(请用逗号分隔) | 申请时间与流程，多个用英文逗号分隔        | TEXT[]（逗号分隔）          |

### 任务5【完成接口和功能概述文档】
创建接口和功能概述文档，命名为：order5.md,放到：Project-HaiFeng\Products，这个下面，最开始也就是开头要写清楚，4个xlsx的表头准确名称（博涵sheet）

### 分页注意
需支持批量删除的功能

### XLSX注意事项：

1. 一个是主表，其他的都是外键，有一对多的，有1对1的，1对1的不能使得外键或者能代替外键的重复，
2. 有外键的xlsx都不会传外键id，而是能代替外键唯一的字段，比如大学名，城市名，需要联表找出id并传入
3. 关于如何识别哪一列跟数据库相对的问题，我认为处理方案是按名称，使用 EasyExcel 的 @ExcelProperty("院校名称") 注解即可，针对这次的名称，结合注释等信息自己决定就可以，后续列出来即可。
4. text[]字段的值必须用逗号格式，支持中英文逗号分隔，意思是逗号分隔，代表是不同的值
````
可以写一个自定义的 Converter：

Java
// 伪代码示例
public class StringToArrayConverter implements Converter<String[]> {
@Override
public String[] convertToJavaData(ReadCellData<?> cellData, ...) {
String val = cellData.getStringValue();
if (val == null) return null;
// 用正则同时兼容中英文逗号
return val.split("[,，]");
}
}
然后在 DTO 字段上加上 @ExcelProperty(converter = StringToArrayConverter.class) 即可。
````
5. 可能涉及到类型转换的问题：基础类型（如 Excel 里的 "2026-05-01" 转 Date，"1000" 转 Integer），EasyExcel 和 Spring Boot 默认就能完美转换。
6. 上传过程中遇到了错误，报第几行的具体错误，比如类型错误呀什么的
7. 使用事务保证一致性
8. 如果内容遇到了空值就默认是null，让数据库判断去吧。意思就是除了数据库指定不能为空之外，其他都可以为空
9. 正确顺序：传入的数据表，根据接口不同，有的是主表，有的不是主表
- 主表：检查表头规范性（比如检查看是否有超过规定的表头），检查unique字段的这一列是否有重复的,正确的话就传入数据
- 外键表：先检查院校名（1对1的不能重复，外键的必须在主表里面有对应的）不能重复检查外键表是否有数据，没有数据就报错，有数据就继续
- 错误处理：明确说清楚是什么报错了，比如哪一行，哪个表头，要明确信息
- 最佳实践：这就需要用到 Spring 的事务一致性。
````
在解析 Excel 的过程中，如果有校验不通过的（比如“第5行：院校名称已存在”），把这些错误信息收集到一个 List<String> errorMsgs 里。
如果 errorMsgs 不为空，直接抛出一个自定义异常 throw new BusinessException(String.join(";", errorMsgs))，并中断解析。
只要你的 Service 方法上加了 @Transactional(rollbackFor = Exception.class)，抛出异常后，前面插入的几行数据会自动全部回滚。
````
10. JSONB需要在原表的的基础上新建sheet1，sheet2等：Sheet 0：基础主表 (必须存在)，Sheet 1：附属labs_jsonb 字段（需要有逻辑外键）
11. 解决服务器压力问题：
配置文件中新增：
spring:
servlet:
multipart:
max-file-size: 30MB      # 限制单个文件最大为 30MB (这个参数合理吗？)
max-request-size: 40MB   # 限制整个请求最大为 40MB (这个参数合理吗？)
11. 使用mybatis-plus的saveBatch，应该是需要开启的
