## 四、后端动态判断引擎（Java）

Java

```
// ============================================================
// 约束判断引擎 - 核心类
// 从数据库读取规则，动态判断用户是否命中约束
// ============================================================

@Service
public class ConstraintEvaluator {

    // ===== 运算符枚举 =====
    private enum Operator {
        EQ,        // 等于
        NEQ,       // 不等于
        LT,        // 小于
        LTE,       // 小于等于
        GT,        // 大于
        GTE,       // 大于等于
        IS_TRUE,   // 布尔为真
        IS_FALSE,  // 布尔为假
        IN,        // 在列表中
        NOT_IN,    // 不在列表中
        OUT_RANGE  // 超出范围（如BMI）
    }

    // ===== 主方法：判断用户命中了哪些约束代码 =====
    public Set<String> getHitConstraintCodes(
            MemberGaokao gaokao,
            MemberProfile profile,
            List<ConstraintDict> allConstraints) {

        Set<String> hitCodes = new HashSet<>();

        for (ConstraintDict rule : allConstraints) {
            if (!rule.isActive()) continue;

            // 先检查附加条件（extra_field），不满足附加条件就跳过整个规则
            if (hasExtraCondition(rule)) {
                if (!evaluateExtraCondition(rule, gaokao, profile)) {
                    continue;  // 附加条件不满足，跳过（例：规则是男生<170，用户是女生）
                }
            }

            // 判断主条件
            if (evaluateMainCondition(rule, gaokao, profile)) {
                hitCodes.add(rule.getCode());
            }
        }

        return hitCodes;
    }

    // ===== 判断主条件 =====
    private boolean evaluateMainCondition(
            ConstraintDict rule,
            MemberGaokao gaokao,
            MemberProfile profile) {

        String field = rule.getCheckField();
        String op = rule.getCheckOperator();
        String value = rule.getCheckValue();

        // 获取用户字段值（gaokao 或 profile）
        Object userValue = getFieldValue(field, gaokao, profile);
        if (userValue == null) return false;  // 字段为空，不做判断

        return evaluate(userValue, Operator.valueOf(op), value, field);
    }

    // ===== 判断附加条件 =====
    private boolean evaluateExtraCondition(
            ConstraintDict rule,
            MemberGaokao gaokao,
            MemberProfile profile) {

        Object userValue = getFieldValue(rule.getExtraField(), gaokao, profile);
        if (userValue == null) return false;

        return evaluate(userValue, Operator.valueOf(rule.getExtraOperator()),
                       rule.getExtraValue(), rule.getExtraField());
    }

    // ===== 核心：执行单个条件判断 =====
    private boolean evaluate(Object userValue, Operator op, String ruleValue, String field) {
        switch (op) {
            case IS_TRUE:
                return Boolean.TRUE.equals(userValue);

            case IS_FALSE:
                return Boolean.FALSE.equals(userValue);

            case EQ:
                return userValue.toString().equals(ruleValue);

            case NEQ:
                return !userValue.toString().equals(ruleValue);

            case LT: {
                // 特殊处理 BMI（计算字段，不在 gaokao 里）
                double v = parseDouble(userValue, field);
                return v < Double.parseDouble(ruleValue);
            }

            case LTE: {
                double v = parseDouble(userValue, field);
                return v <= Double.parseDouble(ruleValue);
            }

            case GT: {
                double v = parseDouble(userValue, field);
                return v > Double.parseDouble(ruleValue);
            }

            case GTE: {
                double v = parseDouble(userValue, field);
                return v >= Double.parseDouble(ruleValue);
            }

            case IN: {
                // IN：用户值在规则列表中
                List<String> values = Arrays.asList(ruleValue.split(","));
                return values.contains(userValue.toString());
            }

            case NOT_IN: {
                List<String> values = Arrays.asList(ruleValue.split(","));
                return !values.contains(userValue.toString());
            }

            case OUT_RANGE: {
                // 格式：min,max
                String[] parts = ruleValue.split(",");
                double min = Double.parseDouble(parts[0]);
                double max = Double.parseDouble(parts[1]);
                double v = parseDouble(userValue, field);
                return v < min || v > max;
            }

            default:
                return false;
        }
    }

    // ===== 从 gaokao/profile 对象反射获取字段值 =====
    private Object getFieldValue(String field, MemberGaokao gaokao, MemberProfile profile) {
        if (field == null) return null;

        // 特殊计算字段
        if ("bmi".equals(field)) {
            return calcBMI(gaokao);
        }
        if ("gender".equals(field)) {
            return profile != null ? profile.getGender() : null;
        }

        // gaokao 字段（Java 反射）
        try {
            Field f = MemberGaokao.class.getDeclaredField(
                toCamelCase(field)  // 下划线转驼峰
            );
            f.setAccessible(true);
            return f.get(gaokao);
        } catch (Exception e) {
            return null;
        }
    }

    private Double calcBMI(MemberGaokao gaokao) {
        if (gaokao.getHeightCm() == null || gaokao.getWeightKg() == null
            || gaokao.getHeightCm() == 0) return null;
        double heightM = gaokao.getHeightCm() / 100.0;
        return gaokao.getWeightKg().doubleValue() / (heightM * heightM);
    }

    private double parseDouble(Object value, String field) {
        if (value instanceof Number) return ((Number) value).doubleValue();
        return Double.parseDouble(value.toString());
    }

    private String toCamelCase(String snakeCase) {
        StringBuilder sb = new StringBuilder();
        boolean nextUpper = false;
        for (char c : snakeCase.toCharArray()) {
            if (c == '_') { nextUpper = true; }
            else if (nextUpper) { sb.append(Character.toUpperCase(c)); nextUpper = false; }
            else { sb.append(c); }
        }
        return sb.toString();
    }

    private boolean hasExtraCondition(ConstraintDict rule) {
        return rule.getExtraField() != null && rule.getExtraOperator() != null;
    }
}
```

---

## 五、约束合并逻辑（回答问题②）

Java

```
// ============================================================
// 约束合并 Service
// ============================================================

@Service
public class ConstraintMergeService {

    // 查某专业组的合并约束（专业组constraints ∪ 所有专业类别constraints ∪ 所有专业明细constraints）
    public Set<String> getMergedGroupConstraints(Integer groupId) {
        Set<String> merged = new HashSet<>();

        // 1. 专业组自身的静态约束
        List<String> groupConstraints = admissionGroupMapper.getConstraints(groupId);
        merged.addAll(groupConstraints);

        // 2. 该组下所有专业明细的约束（TEXT[]字段）
        List<String> majorConstraints = admissionMajorMapper.getAllConstraintsByGroupId(groupId);
        merged.addAll(majorConstraints);

        // 3. 该组下所有专业通过 t_major_constraint 关联的约束
        List<String> staticMajorConstraints = majorConstraintMapper.getConstraintsByGroupId(groupId);
        merged.addAll(staticMajorConstraints);

        return merged;  // 去重并集
    }
}
```

SQL 层面的合并：

SQL

```
-- ============================================================
-- 查某专业组的合并约束（全部来源的并集）
-- ============================================================

WITH group_constraints AS (
    -- 来源1：专业组自身的 constraints 数组
    SELECT unnest(constraints) AS code
    FROM t_admission_group
    WHERE id = 42

    UNION

    -- 来源2：该组下专业明细的 constraints 数组
    SELECT unnest(ams.constraints)
    FROM t_admission_major_score ams
    WHERE ams.group_id = 42

    UNION

    -- 来源3：专业明细关联到 t_major，再查 t_major_constraint
    SELECT mc.constraint_code
    FROM t_admission_major_score ams
        JOIN t_major_constraint mc ON mc.major_code = ams.major_code
    WHERE ams.group_id = 42
)
SELECT DISTINCT code
FROM group_constraints
WHERE code IS NOT NULL;

-- 结果示例：
-- NO_COLOR_BLIND
-- NO_COLOR_BLIND_WEAK
-- ONLY_ENGLISH
```

---

## 六、完整查询流程

Java

```
// ============================================================
// 专业组列表查询 Service
// ============================================================

@Service
public class AdmissionGroupQueryService {

    @Autowired ConstraintEvaluator constraintEvaluator;
    @Autowired ConstraintMergeService constraintMergeService;
    @Autowired ConstraintDictMapper constraintDictMapper;

    public PageResult<AdmissionGroupVO> queryGroups(QueryParam param, Integer memberId) {

        // ===== 1. 查用户档案 =====
        MemberGaokao gaokao = gaokaoMapper.getByMemberId(memberId);
        MemberProfile profile = profileMapper.getByMemberId(memberId);

        // ===== 2. 加载所有约束规则（从字典表，可缓存） =====
        List<ConstraintDict> allRules = constraintDictMapper.findAllActive();

        // ===== 3. 计算用户命中的约束代码集合 =====
        Set<String> userHitCodes = constraintEvaluator.getHitConstraintCodes(
            gaokao, profile, allRules
        );
        // 例：{NO_COLOR_BLIND, NO_COLOR_BLIND_WEAK, ONLY_ENGLISH}

        // ===== 4. 查专业组列表 =====
        List<AdmissionGroup> groups = admissionGroupMapper.queryList(param);

        // ===== 5. 对每个专业组判断约束 =====
        return groups.stream().map(group -> {

            // 拿该组的合并约束
            Set<String> groupConstraints = constraintMergeService
                .getMergedGroupConstraints(group.getId());

            // 计算交集
            Set<String> hitIntersection = new HashSet<>(groupConstraints);
            hitIntersection.retainAll(userHitCodes);

            // 判断展示方式
            AdmissionGroupVO vo = convertToVO(group);

            if (!hitIntersection.isEmpty()) {
                // 判断是 HARD 还是 SOFT
                boolean hasHard = hitIntersection.stream()
                    .anyMatch(code -> allRules.stream()
                        .anyMatch(r -> r.getCode().equals(code)
                               && "HARD".equals(r.getSeverity())));

                if (hasHard) {
                    // HARD：不展示（或展示但标红不可报）
                    vo.setConstraintStatus("BLOCKED");
                } else {
                    // SOFT：展示但标注警告
                    vo.setConstraintStatus("WARNING");
                }

                // 命中的约束详情（前端展示原因）
                vo.setHitConstraints(getConstraintDetails(hitIntersection, allRules));
            } else {
                vo.setConstraintStatus("OK");
            }

            return vo;
        }).collect(Collectors.toList());
    }
}
```

---

## 七、管理端加约束的界面

text

```
╔══════════════════════════════════════════════════════════╗
║  约束条件管理                               [+ 新增约束] ║
╠══════════════════════════════════════════════════════════╣
║  约束代码    名称          大类      严重   操作         ║
║  ─────────────────────────────────────────────────────── ║
║  NO_COLOR..  色盲不可报   身体视觉  HARD   [编辑][删除]  ║
║  VISION_48.. 裸眼视力4.8 身体视觉  HARD   [编辑][删除]  ║
║  ONLY_ENGL.. 只招英语    语种限制  HARD   [编辑][删除]  ║
╠══════════════════════════════════════════════════════════╣
║  [+ 新增约束]                                           ║
║  ┌──────────────────────────────────────────────────┐   ║
║  │ 约束代码：[NO_SMELL_DISORDER    ]                 │   ║
║  │ 名称：    [嗅觉迟钝不可报       ]                 │   ║
║  │ 大类：    [身体视觉 ▼]                            │   ║
║  │ 严重程度：[HARD ▼]                                │   ║
║  │ 说明：    [嗅觉迟钝不能录取...  ]                 │   ║
║  │                                                   │   ║
║  │ 判断规则：                                        │   ║
║  │   档案字段：[has_smell_disorder ▼]                │   ║
║  │   运算符：  [IS_TRUE ▼]                           │   ║
║  │   判断值：  [（留空）]                             │   ║
║  │                                                   │   ║
║  │ 附加条件（可选）：                                │   ║
║  │   附加字段：[（留空） ▼]                          │   ║
║  │   运算符：  [（留空）]                            │   ║
║  │   判断值：  [（留空）]                            │   ║
║  │                                                   │   ║
║  │                           [保存] [取消]           │   ║
║  └──────────────────────────────────────────────────┘   ║
╚══════════════════════════════════════════════════════════╝
```

管理员在这里：

- **档案字段** 下拉：从 `t_member_gaokao` 字段中选
- **运算符** 下拉：EQ / NEQ / LT / GT / IS_TRUE / IS_FALSE / IN / OUT_RANGE
- **判断值** 文本框：填具体值

**完全不需要碰代码，加一条数据就生效。**

---

## 八、专业关联约束管理

text

```
╔══════════════════════════════════════════════════════════╗
║  专业约束配置 > 临床医学 (100201)                        ║
╠══════════════════════════════════════════════════════════╣
║  已有约束：                                              ║
║  ┌──────────────────────────────────────────────────┐   ║
║  │ NO_COLOR_BLIND_WEAK  色盲色弱均不可报   HARD  [删] │   ║
║  │ NO_SMELL_DISORDER    嗅觉迟钝不可报     HARD  [删] │   ║
║  │ ONLY_FRESH          只招应届生         HARD  [删] │   ║
║  └──────────────────────────────────────────────────┘   ║
║                                                          ║
║  [+ 添加约束]                                            ║
║  约束：[NO_TATTOO - 不能有纹身 ▼]  [确认添加]            ║
╚══════════════════════════════════════════════════════════╝
```

---

## 九、约束的缓存优化

Java

```
// 约束规则字典变化不频繁，用 Spring Cache 缓存
@Service
public class ConstraintDictService {

    @Cacheable(value = "constraintRules", key = "'all_active'")
    public List<ConstraintDict> getAllActiveRules() {
        return constraintDictMapper.findAllActive();
    }

    // 管理员修改约束后清缓存
    @CacheEvict(value = "constraintRules", allEntries = true)
    public void saveConstraint(ConstraintDict dict) {
        constraintDictMapper.save(dict);
    }
}
```