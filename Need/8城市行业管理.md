# 总的项目需求

## 数据库表：

### flyway已实现的数据库表：
地址：Project-HaiFeng\haifeng-admin\src\main\resources\db\migration

#### 实现好的
1. admin_logs
2. sys_role
3. sys_module
4. sys_role_module
5. sys_admin
6. t_member
7. 会员订单表 (member_orders)
8. 推荐佣金表 (t_referral_commission)
9. 消息通知表 (t_member_notification)
10. 系统设置表 (system_settings)
11. 公告表 (t_announcements)
12. 规划师表 (t_planners)
13. 培训机构表 (t_institutions)
14. 院校表 (universities)
15. 院校详情表 (universities_detail)
16. 校园图册表（t_campus_gallery）
17. 院校适应指南表 (university_guides)
18. 实验室表 (laboratories)
19. 院系主表 (t_department)
20. 院系分析报告表 (department_reports)
21. 学科评估表 (t_subject_evaluation)
22. 专业表 (t_major)
23. 专业详情表（t_major_detail）
24. 考研专业表 (t_postgrad_major)

#### 新增表
1. 城市表 (t_city)
2. 城市详情表 (t_city_detail)
3. 行业表 (t_industry)
4. 行业详情表 (t_industry_detail)
5. 资源表 (t_resource)

#### 原始表
位置：Project-HaiFeng\haifeng-admin\src\main\resources\db\migration\table\DataTable8.md

#### 待实现模块

城市管理[父模块]--城市列表[子模块]--企业行业关联[子模块]
行业管理[父模块]--行业列表[子模块]
资源管理[父模块]--资源列表[子模块]

### 待实现需求：
**仅限管理员身份**

1. 分页展示城市表，右边三个按钮：详情、禁用、删除
2. 点击查看详情，tabs展示城市和城市详情表，事务一致性保证数据一致
3. 实现一个功能：支持导入两个xlsx，实现了批量导入数据的功能，后续会详细介绍
4. 实现分页查询行业表 (t_postgrad_major),支持增删改查,右边有删除，查看详情按钮（查和修改），禁用。
5. 实现一个导入xlsx批量导入数据库的功能，需要实现两个传入xlsx的接口，对应两个数据库表
6. 分页展示：企业-行业关联表 (t_enterprise_industry)，支持增删改查
7. 实现传入xlsx接口，实现批量导入数据
8. 实现资源表的分页展示，这个简单
### 分页注意

1. 所有模块都要实现批量删除的功能，注意：这是硬删除
2. 支持两个删除，一个是软删除，另一个是硬删除，在列表旁边即可，（命名为：弃用、删除）
3. 删除的可以恢复，（也就是status可以变为1或is_deleted = false），否则数据库会越来越多，新建一个新的还外键和主键在而建不了
4. 所有列表右边都有三个按钮：1.删除（硬删除） 2.禁用（软删除，重新点击后恢复） 3. 详情（点击可以查看所有和修改）

### 任务1【完成数据表】
根据db\migration\table\DataTable8.md的数据库表，创建并完善V8__create_cities_industries.sql,放到flyway的迁移下面，要注意索引，注释等规范

### 任务2【完成城市表 (t_city)】
1. 列表展示：city_name,province,college_count，key_college_count，resident_population，is_deleted，右边有详情，删除，禁用
2. 点击详情，展示专业表和专业详情表的所有字段（tabs分开，他们是一对一的关系）
3. 实现xlsx批量导入功能，两个xlsx（JSONB字段要用sheet）
4. 模糊查询：city_name，province，region，is_deleted

专业详情表里面的JSONB字段：
城市详情_产业结构（对应字段名：industry_structure）
| 字段名       | 说明                     | 类型/约束                |
| --------- | ---------------------- | -------------------- |
| 城市名称      | 城市名称（外键，填写城市详情表中的城市名称） | VARCHAR(50) NOT NULL |
| 第一产业占比(%) | 第一产业占比，范围0~100         | NUMERIC(5,2)         |
| 第二产业占比(%) | 第二产业占比，范围0~100         | NUMERIC(5,2)         |
| 第三产业占比(%) | 第三产业占比，范围0~100         | NUMERIC(5,2)         |

城市详情_房价水平（housing_price_level）
| 字段名          | 说明                     | 类型/约束                |
| ------------ | ---------------------- | -------------------- |
| 城市名称         | 城市名称（外键，填写城市详情表中的城市名称） | VARCHAR(50) NOT NULL |
| 平均房价(万元/㎡)   | 城市平均房价                 | NUMERIC(10,2)        |
| 核心区房价(万元/㎡)  | 核心区域房价                 | NUMERIC(10,2)        |
| 郊区房价范围(万元/㎡) | 郊区房价范围，因为是区间用字符串表示     | VARCHAR(20)          |
| 房价涨幅(%)      | 房价涨幅百分比                | NUMERIC(5,2)         |
| 房价收入比(%)     | 房价收入比                  | NUMERIC(5,2)         |

城市详情_高等教育资源（high_education）
| 字段名      | 说明                     | 类型/约束                |
| -------- | ---------------------- | -------------------- |
| 城市名称     | 城市名称（外键，填写城市详情表中的城市名称） | VARCHAR(50) NOT NULL |
| 高校总数     | 该城市高校总数                | INTEGER              |
| 双一流高校数量  | 985/211/双一流高校数量        | INTEGER              |
| 在校生数量(万) | 在校本科生数量（万人），前端拼接"万+"   | INTEGER              |
| 研究生数量(万) | 在校研究生数量（万人），前端拼接"万"    | INTEGER              |

城市详情_基础教育资源（basic_education）
| 字段名    | 说明                     | 类型/约束                |
| ------ | ---------------------- | -------------------- |
| 城市名称   | 城市名称（外键，填写城市详情表中的城市名称） | VARCHAR(50) NOT NULL |
| 学校总数   | 中小学等基础教育学校总数           | INTEGER              |
| 示范学校数量 | 示范学校数量                 | INTEGER              |
| 重点学校数量 | 重点学校数量                 | INTEGER              |
| 教育备注   | 教育资源备注说明               | TEXT                 |

城市详情_交通数据（transportation）
| 字段名        | 说明                     | 类型/约束                |
| ---------- | ---------------------- | -------------------- |
| 城市名称       | 城市名称（外键，填写城市详情表中的城市名称） | VARCHAR(50) NOT NULL |
| 地铁线路(条)    | 地铁线路数量                 | INTEGER              |
| 地铁里程(公里)   | 地铁运营里程                 | INTEGER              |
| 高速公路里程(公里) | 高速公路里程                 | INTEGER              |
| 交通世界排名     | 交通水平世界排名               | INTEGER              |

城市详情_就业数据（employment）
| 字段名         | 说明                     | 类型/约束                |
| ----------- | ---------------------- | -------------------- |
| 城市名称        | 城市名称（外键，填写城市详情表中的城市名称） | VARCHAR(50) NOT NULL |
| 城市失业率(%)    | 城市失业率                  | NUMERIC(5,2)         |
| 全国平均失业率(%)  | 全国平均失业率                | NUMERIC(5,2)         |
| 第三产业就业占比(%) | 第三产业就业占比               | NUMERIC(5,2)         |
| 新增就业(万人)    | 新增就业人数（万人）             | NUMERIC(10,2)        |
| 平均工资(万元/年)  | 平均工资（万元/年）             | NUMERIC(10,2)        |
| 工资排名(全国)    | 全国工资排名                 | INTEGER              |
| 技能人才占比(%)   | 技能人才占比                 | NUMERIC(5,2)         |
| 技能人才增长(%)   | 技能人才增长率                | NUMERIC(5,2)         |

城市详情_企业统计（enterprise_stats）
| 字段名        | 说明                     | 类型/约束                |
| ---------- | ---------------------- | -------------------- |
| 城市名称       | 城市名称（外键，填写城市详情表中的城市名称） | VARCHAR(50) NOT NULL |
| 企业类别数      | 企业类别数量                 | INTEGER              |
| 重点企业总数     | 重点企业总数量                | INTEGER              |
| 世界500强企业数量 | 世界500强企业入驻数量           | INTEGER              |

城市详情_未来规划（future_plan）
| 字段名        | 说明                     | 类型/约束                |
| ---------- | ---------------------- | -------------------- |
| 城市名称       | 城市名称（外键，填写城市详情表中的城市名称） | VARCHAR(50) NOT NULL |
| 目标年份       | 规划目标年份                 | INTEGER              |
| 发展目标       | 城市发展目标描述               | TEXT                 |
| 重点领域(逗号分隔) | 重点发展领域列表，多个用英文逗号分隔     | TEXT[]（逗号分隔）         |

城市详情_文化旅游数据（culture）
| 字段名        | 说明                        | 类型/约束                |
| ---------- | ------------------------- | -------------------- |
| 城市名称       | 城市名称（外键，填写城市详情表中的城市名称）    | VARCHAR(50) NOT NULL |
| 世界遗产数量(项)  | 世界遗产数量                    | INTEGER              |
| 年游客量(万人次)  | 年游客量（万人次），前端拼接"亿"需除以10000 | INTEGER              |
| A级景区数量(家)  | A级景区数量                    | INTEGER              |
| 核心景点(逗号分隔) | 核心景点名称列表，多个用英文逗号分隔        | TEXT[]（逗号分隔）         |

城市详情_消费数据说明（consumption）
| 字段名        | 说明                     | 类型/约束                |
| ---------- | ---------------------- | -------------------- |
| 城市名称       | 城市名称（外键，填写城市详情表中的城市名称） | VARCHAR(50) NOT NULL |
| 人均消费(万元/年) | 人均年消费金额                | NUMERIC(10,2)        |
| 消费涨幅(%)    | 消费涨幅百分比                | NUMERIC(5,2)         |
| 恩格尔系数(%)   | 恩格尔系数                  | NUMERIC(5,2)         |
| 教育支出占比(%)  | 教育支出占比                 | NUMERIC(5,2)         |
| 消费指数       | 消费指数（倍数，1为基准）          | NUMERIC(5,2)         |
| 消费排名(全国)   | 全国消费排名                 | INTEGER              |

城市详情_医疗数据（medical）
| 字段名        | 说明                     | 类型/约束                |
| ---------- | ---------------------- | -------------------- |
| 城市名称       | 城市名称（外键，填写城市详情表中的城市名称） | VARCHAR(50) NOT NULL |
| 三甲医院数量(所)  | 三甲医院数量                 | INTEGER              |
| 三级医院总数(所)  | 三级医院总数                 | INTEGER              |
| 医生密度(人/千人) | 每千人医生数量                | NUMERIC(5,2)         |
| 医疗排名(全国)   | 全国医疗水平排名               | INTEGER              |

城市详情_住房政策（housing_policy）
| 字段名       | 说明                     | 类型/约束                |
| --------- | ---------------------- | -------------------- |
| 城市名称      | 城市名称（外键，填写城市详情表中的城市名称） | VARCHAR(50) NOT NULL |
| 限购政策      | 限购政策描述                 | TEXT                 |
| 共有产权房(万套) | 共有产权房数量（万套）            | NUMERIC(10,2)        |
| 公租房(万套)   | 公租房数量（万套）              | NUMERIC(10,2)        |
| 首套房利率(%)  | 首套房贷款利率                | NUMERIC(5,2)         |
| 二套房利率(%)  | 二套房贷款利率                | NUMERIC(5,2)         |

城市详情_租房成本（rental_cost）
| 字段名          | 说明                     | 类型/约束                |
| ------------ | ---------------------- | -------------------- |
| 城市名称         | 城市名称（外键，填写城市详情表中的城市名称） | VARCHAR(50) NOT NULL |
| 市中心租金范围(元/月) | 市中心租金范围，区间用字符串表示       | VARCHAR(20)          |
| 郊区租金范围(元/月)  | 郊区租金范围，区间用字符串表示        | VARCHAR(20)          |
| 租金收入比(%)     | 租金收入比                  | NUMERIC(5,2)         |
| 租金涨幅(%)      | 租金涨幅百分比                | NUMERIC(5,2)         |



### 任务3【行业表 (t_industry)】

1. 展示列表：industry_name，category，talent_trend，annual_growth_rate，is_deleted。支持修改，禁用，删除
2. 点击详情，展示行业表和行业详情表的所有字段（tabs分开，他们是一对一的关系）
3. 实现xlsx批量导入功能，两个xlsx（JSONB字段要用sheet）
4. 模糊查询：industry_name，category，region，talent_trend，is_deleted

行业详情表里面的JSONB字段：

行业详情_发展地域支持(development_support_info)
| 字段名        | 说明                     | 类型/约束                 |
| ---------- | ---------------------- | --------------------- |
| 行业名称       | 行业名称（外键，填写行业详情表中的行业名称） | VARCHAR(100) NOT NULL |
| 地域发展概述     | 地域发展概述描述               | VARCHAR(100)          |
| 重点城市(逗号分隔) | 重点发展城市列表，多个用英文逗号分隔     | TEXT[]（逗号分隔）          |
| 城市政策(逗号分隔) | 各城市相关政策列表，多个用英文逗号分隔    | TEXT[]（逗号分隔）          |

行业详情_发展规模说明(industry_scale)
| 字段名          | 说明                     | 类型/约束                 |
| ------------ | ---------------------- | --------------------- |
| 行业名称         | 行业名称（外键，填写行业详情表中的行业名称） | VARCHAR(100) NOT NULL |
| 发展规模(万亿元)    | 发展规模数值，前端拼接"万亿元"       | INTEGER               |
| 发展规模标签       | 发展规模标签说明               | VARCHAR(100)          |
| 发展规模描述(逗号分隔) | 发展规模描述内容，多个用英文逗号分隔     | TEXT[]（逗号分隔）          |


行业详情_人才需求分析说明(talent_analysis)
| 字段名        | 说明                     | 类型/约束                 |
| ---------- | ---------------------- | --------------------- |
| 行业名称       | 行业名称（外键，填写行业详情表中的行业名称） | VARCHAR(100) NOT NULL |
| 分析标题       | 人才需求分析标题               | VARCHAR(50)           |
| 紧缺岗位(逗号分隔) | 紧缺岗位列表，多个用英文逗号分隔       | TEXT[]（逗号分隔）          |
| 学历要求       | 学历要求描述                 | VARCHAR(100)          |
| 专业要求       | 专业要求描述                 | VARCHAR(100)          |
| 人才趋势描述     | 人才趋势详细描述               | TEXT                  |


行业详情_人才需求说明(industry_talent_demand)
| 字段名          | 说明                     | 类型/约束                 |
| ------------ | ---------------------- | --------------------- |
| 行业名称         | 行业名称（外键，填写行业详情表中的行业名称） | VARCHAR(100) NOT NULL |
| 人才需求(万人)     | 人才需求数值，前端拼接"万人"        | INTEGER               |
| 人才需求标签       | 人才需求标签说明               | VARCHAR(100)          |
| 人才需求描述(逗号分隔) | 人才需求描述内容，多个用英文逗号分隔     | TEXT[]（逗号分隔）          |


行业详情_人才引进政策说明(talent_policy)
| 字段名         | 说明                     | 类型/约束                 |
| ----------- | ---------------------- | --------------------- |
| 行业名称        | 行业名称（外键，填写行业详情表中的行业名称） | VARCHAR(100) NOT NULL |
| 政策标题        | 人才引进政策标题               | VARCHAR(50)           |
| 国家级政策(逗号分隔) | 国家级政策列表，多个用英文逗号分隔      | TEXT[]（逗号分隔）          |
| 地方级政策(逗号分隔) | 地方级政策列表，多个用英文逗号分隔      | TEXT[]（逗号分隔）          |
| 企业层面描述      | 企业层面政策描述               | TEXT                  |



行业详情_薪资数据说明(salary_data)
| 字段名      | 说明                     | 类型/约束                 |
| -------- | ---------------------- | --------------------- |
| 行业名称     | 行业名称（外键，填写行业详情表中的行业名称） | VARCHAR(100) NOT NULL |
| 薪资分析标题   | 薪资分析标题                 | VARCHAR(100)          |
| 薪资分析描述   | 薪资分析详细描述               | TEXT                  |
| 地域薪资差异标题 | 地域薪资差异分析标题             | VARCHAR(50)           |
| 地域薪资差异描述 | 地域薪资差异详细描述             | TEXT                  |
| 薪资趋势分析   | 薪资趋势分析内容               | TEXT                  |


行业详情_行业薪资说明(industry_salary)
| 字段名        | 说明                     | 类型/约束                 |
| ---------- | ---------------------- | --------------------- |
| 行业名称       | 行业名称（外键，填写行业详情表中的行业名称） | VARCHAR(100) NOT NULL |
| 薪资范围(万元)   | 薪资范围，前端拼接"万元"          | VARCHAR(50)           |
| 薪资标签       | 薪资标签说明                 | VARCHAR(100)          |
| 薪资描述(逗号分隔) | 薪资描述内容，多个用英文逗号分隔       | TEXT[]（逗号分隔）          |


行业详情_政策信息说明(policy_info)
| 字段名        | 说明                     | 类型/约束                 |
| ---------- | ---------------------- | --------------------- |
| 行业名称       | 行业名称（外键，填写行业详情表中的行业名称） | VARCHAR(100) NOT NULL |
| 政策概览       | 政策概览描述                 | VARCHAR(100)          |
| 国家政策(逗号分隔) | 国家政策列表，多个用英文逗号分隔       | TEXT[]（逗号分隔）          |
| 政策亮点       | 政策亮点描述                 | TEXT                  |


### 任务4【资源表 (t_resource)】

1. 列表展示：resource_name，category，file_type，view_count，sort_order，is_deleted
2. 实现模糊查询：resource_name，category，is_deleted


### 任务5【完成接口和功能概述文档】
创建接口和功能概述文档，命名为：order8.md,放到：Project-HaiFeng\Products，这个下面，最开始也就是开头要写清楚，4个xlsx的表头（EasyExcel定义的）准确名称（包含sheet的,没有JSONB不需要）


### XLSX注意事项：

1. 一个是主表，其他的都是外键，有一对多的，有1对1的，1对1的不能使得外键或者能代替外键的重复，
2. 有外键的xlsx都不会传外键id，而是能代替外键唯一的字段，比如大学名，城市名，需要联表找出id并传入
3. 关于如何识别哪一列跟数据库相对的问题，我认为处理方案是按名称，使用 EasyExcel 的 @ExcelProperty("院校名称") 注解即可，针对这次的名称，结合注释等信息自己决定就可以，后续列出来即可。
4. text[]字段的值必须用逗号格式，支持中英文逗号分隔，意思是逗号分隔，代表是不同的值
````
可以写一个自定义的 Converter：

Java
// 伪代码示例
public class StringToArrayConverter implements Converter<String[]> {
@Override
public String[] convertToJavaData(ReadCellData<?> cellData, ...) {
String val = cellData.getStringValue();
if (val == null) return null;
// 用正则同时兼容中英文逗号
return val.split("[,，]");
}
}
然后在 DTO 字段上加上 @ExcelProperty(converter = StringToArrayConverter.class) 即可。
````
5. 可能涉及到类型转换的问题：基础类型（如 Excel 里的 "2026-05-01" 转 Date，"1000" 转 Integer），EasyExcel 和 Spring Boot 默认就能完美转换。
6. 上传过程中遇到了错误，报第几行的具体错误，比如类型错误呀什么的
7. 使用事务保证一致性
8. 如果内容遇到了空值就默认是null，让数据库判断去吧。意思就是除了数据库指定不能为空之外，其他都可以为空
9. 正确顺序：传入的数据表，根据接口不同，有的是主表，有的不是主表
- 主表：检查表头规范性（比如检查看是否有超过规定的表头），检查unique字段的这一列是否有重复的,正确的话就传入数据
- 外键表：先检查院校名（1对1的不能重复，外键的必须在主表里面有对应的）不能重复检查外键表是否有数据，没有数据就报错，有数据就继续
- 错误处理：明确说清楚是什么报错了，比如哪一行，哪个表头，要明确信息
- 最佳实践：这就需要用到 Spring 的事务一致性。
````
在解析 Excel 的过程中，如果有校验不通过的（比如“第5行：院校名称已存在”），把这些错误信息收集到一个 List<String> errorMsgs 里。
如果 errorMsgs 不为空，直接抛出一个自定义异常 throw new BusinessException(String.join(";", errorMsgs))，并中断解析。
只要你的 Service 方法上加了 @Transactional(rollbackFor = Exception.class)，抛出异常后，前面插入的几行数据会自动全部回滚。
````
10. JSONB需要在原表的的基础上新建sheet1，sheet2等：Sheet 0：基础主表 (必须存在)，Sheet 1：附属labs_jsonb 字段（需要有逻辑外键）


### 功能统一需求

1. 统一用雪花算法
2. 按子模块独立Controller，每个子模块独立Controller，复用现有Excel导入模式

### 总结
1. 简单的增删改查，分页展示，1对1的表tabs一起更新或增加
2. 新增了xlsx表，之前实现过，可以复用，在university实现过类似的
3. xlsx表有关jsonb的逻辑，需要新建一个sheet，里面是jsonb字段
