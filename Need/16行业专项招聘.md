# 总的项目需求

#### 需要用到的数据库表
````
-- ============================================================
-- V20__industry_position__tables.sql
-- 模块三：行业专项招聘
-- 包含：教师招聘、医疗卫生、银行/金融
-- 共性：行业垂直细分，各自有独立资质体系
-- ============================================================

BEGIN;

-- ============================================================
-- 1. 教师招聘岗位表 (t_teacher_position)
-- ============================================================
CREATE TABLE IF NOT EXISTS t_teacher_position (
    id                          BIGINT          PRIMARY KEY,

    -- ==================== 学校信息 ====================
    school_name                 VARCHAR(200)    NOT NULL,
    school_type                 VARCHAR(30)     NOT NULL,
    school_nature               VARCHAR(20)     DEFAULT '公办',
    supervising_dept            VARCHAR(200),

    -- ==================== 岗位信息 ====================
    position_name               VARCHAR(200)    NOT NULL,
    subject                     VARCHAR(50)     NOT NULL,
    recruitment_type            VARCHAR(30)     NOT NULL,

    -- ==================== 地区信息 ====================
    province                    VARCHAR(30)     NOT NULL,
    city                        VARCHAR(50),
    district                    VARCHAR(50),

    -- ==================== 报考要求 ====================
    education_requirement       VARCHAR(30),
    degree_requirement          VARCHAR(30),
    major_requirement           VARCHAR(500),
    age_limit                   INTEGER,
    recruitment_count           INTEGER         DEFAULT 1,

    -- ==================== 教师岗位特殊要求 ====================
    teacher_cert_requirement    VARCHAR(100),
    teacher_cert_subject        VARCHAR(50),
    putonghua_level             VARCHAR(30),
    other_cert_requirement      VARCHAR(200),
    work_experience             VARCHAR(50),
    is_normal_major             VARCHAR(20),

    -- ==================== 待遇信息 ====================
    salary_range                VARCHAR(50),
    benefits                    TEXT,

    -- ==================== 考试信息 ====================
    exam_content                VARCHAR(500),
    interview_form              VARCHAR(100),
    reg_start_date              TIMESTAMPTZ,
    reg_end_date                TIMESTAMPTZ,
    exam_time                   TIMESTAMPTZ,

    -- ==================== 补充信息 ====================
    position_status             VARCHAR(20)     DEFAULT '招聘中',
    apply_link                  VARCHAR(500),
    contact_phone               VARCHAR(50),
    remark                      TEXT,
    content                     TEXT,

    -- ==================== 审计字段 ====================
    sort_order                   INTEGER         DEFAULT 0,
    is_deleted                  BOOLEAN         NOT NULL DEFAULT FALSE,
    created_at                  TIMESTAMPTZ     NOT NULL DEFAULT NOW(),
    updated_at                  TIMESTAMPTZ     NOT NULL DEFAULT NOW(),

    -- ==================== 约束 ====================
    CONSTRAINT chk_tch_school_type CHECK (
        school_type IN ('幼儿园', '小学', '初中', '高中', '中职', '高职', '大学', '特殊教育学校')
    ),
    CONSTRAINT chk_tch_school_nature CHECK (
        school_nature IS NULL OR school_nature IN ('公办', '民办')
    ),
    CONSTRAINT chk_tch_subject CHECK (
        subject IN (
            '语文', '数学', '英语', '物理', '化学', '生物',
            '历史', '地理', '政治', '音乐', '美术', '体育',
            '信息技术', '心理健康', '通用技术', '科学',
            '道德与法治', '综合实践', '学前教育', '特殊教育', '其他'
        )
    ),
    CONSTRAINT chk_tch_recruit_type CHECK (
        recruitment_type IN ('编制', '合同制', '特岗教师', '人事代理', '编外聘用')
    ),
    CONSTRAINT chk_tch_education CHECK (
        education_requirement IS NULL
        OR education_requirement IN ('不限', '大专', '本科', '硕士', '博士')
    ),
    CONSTRAINT chk_tch_age CHECK (
        age_limit IS NULL OR (age_limit >= 18 AND age_limit <= 60)
    ),
    CONSTRAINT chk_tch_recruitment CHECK (
        recruitment_count IS NULL OR recruitment_count > 0
    ),
    CONSTRAINT chk_tch_putonghua CHECK (
        putonghua_level IS NULL
        OR putonghua_level IN ('不限', '二级乙等', '二级甲等', '一级乙等', '一级甲等')
    ),
    CONSTRAINT chk_tch_normal CHECK (
        is_normal_major IS NULL OR is_normal_major IN ('要求', '优先', '不限')
    ),
    CONSTRAINT chk_tch_status CHECK (
        position_status IS NULL OR position_status IN ('招聘中', '已结束', '即将开始')
    )
);

CREATE INDEX idx_tch_subject              ON t_teacher_position (subject) WHERE is_deleted = FALSE;
CREATE INDEX idx_tch_school_type          ON t_teacher_position (school_type) WHERE is_deleted = FALSE;
CREATE INDEX idx_tch_location             ON t_teacher_position (province, city, district) WHERE is_deleted = FALSE;
CREATE INDEX idx_tch_recruit_type         ON t_teacher_position (recruitment_type) WHERE is_deleted = FALSE;
CREATE INDEX idx_tch_province_type_subject ON t_teacher_position (province, school_type, subject) WHERE is_deleted = FALSE;
CREATE INDEX idx_tch_education            ON t_teacher_position (education_requirement) WHERE is_deleted = FALSE;
CREATE INDEX idx_tch_school_nature        ON t_teacher_position (school_nature) WHERE is_deleted = FALSE;
CREATE INDEX idx_tch_pos_name             ON t_teacher_position USING btree (position_name varchar_pattern_ops) WHERE is_deleted = FALSE;
CREATE INDEX idx_tch_school_name          ON t_teacher_position USING btree (school_name varchar_pattern_ops) WHERE is_deleted = FALSE;
CREATE INDEX idx_tch_status               ON t_teacher_position (position_status) WHERE is_deleted = FALSE;
CREATE INDEX idx_tch_reg_end              ON t_teacher_position (reg_end_date ASC NULLS LAST) WHERE is_deleted = FALSE;

CREATE TRIGGER trg_tch_position_updated_at
    BEFORE UPDATE ON t_teacher_position
    FOR EACH ROW EXECUTE FUNCTION fn_update_timestamp();

COMMENT ON TABLE  t_teacher_position                            IS '教师招聘岗位表';
COMMENT ON COLUMN t_teacher_position.school_name                IS '学校名称';
COMMENT ON COLUMN t_teacher_position.school_type                IS '学校类型（幼儿园/小学/初中/高中/中职/高职/大学/特教）';
COMMENT ON COLUMN t_teacher_position.school_nature              IS '学校性质（公办/民办）';
COMMENT ON COLUMN t_teacher_position.supervising_dept           IS '主管教育部门';
COMMENT ON COLUMN t_teacher_position.position_name              IS '岗位名称';
COMMENT ON COLUMN t_teacher_position.subject                    IS '学科';
COMMENT ON COLUMN t_teacher_position.recruitment_type           IS '招聘类型（编制/合同制/特岗教师/人事代理/编外聘用）';
COMMENT ON COLUMN t_teacher_position.province                   IS '省份';
COMMENT ON COLUMN t_teacher_position.city                       IS '城市';
COMMENT ON COLUMN t_teacher_position.district                   IS '区/县';
COMMENT ON COLUMN t_teacher_position.teacher_cert_requirement   IS '教师资格证要求';
COMMENT ON COLUMN t_teacher_position.teacher_cert_subject       IS '资格证学科要求';
COMMENT ON COLUMN t_teacher_position.putonghua_level            IS '普通话等级要求';
COMMENT ON COLUMN t_teacher_position.other_cert_requirement     IS '其他证书要求';
COMMENT ON COLUMN t_teacher_position.work_experience            IS '教学经验要求';
COMMENT ON COLUMN t_teacher_position.is_normal_major            IS '是否要求师范专业（要求/优先/不限）';
COMMENT ON COLUMN t_teacher_position.salary_range               IS '薪资待遇';
COMMENT ON COLUMN t_teacher_position.benefits                   IS '福利待遇';
COMMENT ON COLUMN t_teacher_position.exam_content               IS '笔试内容';
COMMENT ON COLUMN t_teacher_position.interview_form             IS '面试形式（试讲/说课/结构化/答辩）';
COMMENT ON COLUMN t_teacher_position.reg_start_date             IS '报名开始日期';
COMMENT ON COLUMN t_teacher_position.reg_end_date               IS '报名截止日期';
COMMENT ON COLUMN t_teacher_position.exam_time                  IS '考试时间';
COMMENT ON COLUMN t_teacher_position.position_status            IS '状态（招聘中/已结束/即将开始）';
COMMENT ON COLUMN t_teacher_position.apply_link                 IS '报名链接';
COMMENT ON COLUMN t_teacher_position.contact_phone              IS '联系电话';
COMMENT ON COLUMN t_teacher_position.remark                     IS '备注';
COMMENT ON COLUMN t_teacher_position.content                    IS '详细说明（支持HTML）';


-- ============================================================
-- 2. 医疗卫生招聘岗位表 (t_healthcare_position)
-- ============================================================
CREATE TABLE IF NOT EXISTS t_healthcare_position (
    id                          BIGINT          PRIMARY KEY,

    -- ==================== 机构信息 ====================
    institution_name            VARCHAR(200)    NOT NULL,
    institution_type            VARCHAR(50)     NOT NULL,
    institution_level           VARCHAR(30),
    institution_nature          VARCHAR(20)     DEFAULT '公立',

    -- ==================== 岗位信息 ====================
    position_name               VARCHAR(200)    NOT NULL,
    department                  VARCHAR(100),
    position_category           VARCHAR(30)     NOT NULL,
    recruitment_type            VARCHAR(30),

    -- ==================== 地区信息 ====================
    province                    VARCHAR(30)     NOT NULL,
    city                        VARCHAR(50),
    district                    VARCHAR(50),

    -- ==================== 报考要求 ====================
    education_requirement       VARCHAR(30),
    degree_requirement          VARCHAR(30),
    major_requirement           VARCHAR(500),
    age_limit                   INTEGER,
    recruitment_count           INTEGER         DEFAULT 1,
    work_experience             VARCHAR(50),

    -- ==================== 医疗行业特殊要求 ====================
    license_requirement         VARCHAR(100),
    title_requirement           VARCHAR(30),
    internship_requirement      VARCHAR(50),
    research_requirement        TEXT,

    -- ==================== 待遇信息 ====================
    salary_range                VARCHAR(50),
    benefits                    TEXT,
    housing_subsidy             VARCHAR(100),

    -- ==================== 报名信息 ====================
    reg_start_date              TIMESTAMPTZ,
    reg_end_date                TIMESTAMPTZ,
    exam_time                   TIMESTAMPTZ,
    exam_content                VARCHAR(500),
    apply_link                  VARCHAR(500),

    -- ==================== 补充信息 ====================
    position_status             VARCHAR(20)     DEFAULT '招聘中',
    contact_phone               VARCHAR(50),
    contact_person              VARCHAR(50),
    remark                      TEXT,
    content                     TEXT,

    -- ==================== 审计字段 ====================
    sort_order                   INTEGER         DEFAULT 0,
    is_deleted                  BOOLEAN         NOT NULL DEFAULT FALSE,
    created_at                  TIMESTAMPTZ     NOT NULL DEFAULT NOW(),
    updated_at                  TIMESTAMPTZ     NOT NULL DEFAULT NOW(),

    -- ==================== 约束 ====================
    CONSTRAINT chk_hc_inst_type CHECK (
        institution_type IN (
            '综合医院', '专科医院', '中医医院', '社区卫生服务中心',
            '疾控中心', '妇幼保健院', '卫生监督所', '急救中心',
            '血站', '精神卫生中心', '康复中心', '其他'
        )
    ),
    CONSTRAINT chk_hc_inst_level CHECK (
        institution_level IS NULL
        OR institution_level IN ('三级甲等', '三级乙等', '二级甲等', '二级乙等', '一级', '未定级', '社区')
    ),
    CONSTRAINT chk_hc_inst_nature CHECK (
        institution_nature IS NULL OR institution_nature IN ('公立', '民营')
    ),
    CONSTRAINT chk_hc_pos_category CHECK (
        position_category IN ('临床医师', '护理', '药学', '医技', '公共卫生', '行政后勤', '科研')
    ),
    CONSTRAINT chk_hc_recruit_type CHECK (
        recruitment_type IS NULL
        OR recruitment_type IN ('编制', '合同制', '人事代理', '规培', '进修')
    ),
    CONSTRAINT chk_hc_education CHECK (
        education_requirement IS NULL
        OR education_requirement IN ('不限', '大专', '本科', '硕士', '博士')
    ),
    CONSTRAINT chk_hc_title CHECK (
        title_requirement IS NULL
        OR title_requirement IN ('不限', '初级', '中级', '副高级', '正高级')
    ),
    CONSTRAINT chk_hc_age CHECK (
        age_limit IS NULL OR (age_limit >= 18 AND age_limit <= 65)
    ),
    CONSTRAINT chk_hc_recruitment CHECK (
        recruitment_count IS NULL OR recruitment_count > 0
    ),
    CONSTRAINT chk_hc_status CHECK (
        position_status IS NULL OR position_status IN ('招聘中', '已结束', '即将开始')
    )
);

CREATE INDEX idx_hc_pos_category  ON t_healthcare_position (position_category) WHERE is_deleted = FALSE;
CREATE INDEX idx_hc_inst_type     ON t_healthcare_position (institution_type) WHERE is_deleted = FALSE;
CREATE INDEX idx_hc_inst_level    ON t_healthcare_position (institution_level) WHERE is_deleted = FALSE;
CREATE INDEX idx_hc_location      ON t_healthcare_position (province, city) WHERE is_deleted = FALSE;
CREATE INDEX idx_hc_department    ON t_healthcare_position (department) WHERE is_deleted = FALSE;
CREATE INDEX idx_hc_recruit_type  ON t_healthcare_position (recruitment_type) WHERE is_deleted = FALSE;
CREATE INDEX idx_hc_education     ON t_healthcare_position (education_requirement) WHERE is_deleted = FALSE;
CREATE INDEX idx_hc_title         ON t_healthcare_position (title_requirement) WHERE is_deleted = FALSE;
CREATE INDEX idx_hc_composite     ON t_healthcare_position (province, position_category, institution_level) WHERE is_deleted = FALSE;
CREATE INDEX idx_hc_inst_name     ON t_healthcare_position USING btree (institution_name varchar_pattern_ops) WHERE is_deleted = FALSE;
CREATE INDEX idx_hc_pos_name      ON t_healthcare_position USING btree (position_name varchar_pattern_ops) WHERE is_deleted = FALSE;
CREATE INDEX idx_hc_status        ON t_healthcare_position (position_status) WHERE is_deleted = FALSE;

CREATE TRIGGER trg_hc_position_updated_at
    BEFORE UPDATE ON t_healthcare_position
    FOR EACH ROW EXECUTE FUNCTION fn_update_timestamp();

COMMENT ON TABLE  t_healthcare_position                             IS '医疗卫生招聘岗位表';
COMMENT ON COLUMN t_healthcare_position.institution_name            IS '医疗机构名称';
COMMENT ON COLUMN t_healthcare_position.institution_type            IS '机构类型';
COMMENT ON COLUMN t_healthcare_position.institution_level           IS '机构等级（三甲/三乙/二甲/...）';
COMMENT ON COLUMN t_healthcare_position.institution_nature          IS '机构性质（公立/民营）';
COMMENT ON COLUMN t_healthcare_position.position_name               IS '岗位名称';
COMMENT ON COLUMN t_healthcare_position.department                  IS '科室';
COMMENT ON COLUMN t_healthcare_position.position_category           IS '岗位类别（临床医师/护理/药学/医技/公卫/行政/科研）';
COMMENT ON COLUMN t_healthcare_position.recruitment_type            IS '招聘类型（编制/合同制/人事代理/规培/进修）';
COMMENT ON COLUMN t_healthcare_position.province                    IS '省份';
COMMENT ON COLUMN t_healthcare_position.city                        IS '城市';
COMMENT ON COLUMN t_healthcare_position.district                    IS '区/县';
COMMENT ON COLUMN t_healthcare_position.education_requirement       IS '学历要求';
COMMENT ON COLUMN t_healthcare_position.degree_requirement          IS '学位要求';
COMMENT ON COLUMN t_healthcare_position.major_requirement           IS '专业要求';
COMMENT ON COLUMN t_healthcare_position.age_limit                   IS '年龄上限（周岁）';
COMMENT ON COLUMN t_healthcare_position.recruitment_count           IS '招聘人数';
COMMENT ON COLUMN t_healthcare_position.work_experience             IS '工作经验';
COMMENT ON COLUMN t_healthcare_position.license_requirement         IS '执业资格证要求';
COMMENT ON COLUMN t_healthcare_position.title_requirement           IS '职称要求（不限/初级/中级/副高级/正高级）';
COMMENT ON COLUMN t_healthcare_position.internship_requirement      IS '规培要求';
COMMENT ON COLUMN t_healthcare_position.research_requirement        IS '科研要求';
COMMENT ON COLUMN t_healthcare_position.salary_range                IS '薪资待遇';
COMMENT ON COLUMN t_healthcare_position.benefits                    IS '福利待遇';
COMMENT ON COLUMN t_healthcare_position.housing_subsidy             IS '住房补贴/安家费';
COMMENT ON COLUMN t_healthcare_position.reg_start_date              IS '报名开始';
COMMENT ON COLUMN t_healthcare_position.reg_end_date                IS '报名截止';
COMMENT ON COLUMN t_healthcare_position.exam_time                   IS '考试时间';
COMMENT ON COLUMN t_healthcare_position.exam_content                IS '考试内容';
COMMENT ON COLUMN t_healthcare_position.apply_link                  IS '报名链接';
COMMENT ON COLUMN t_healthcare_position.position_status             IS '状态';
COMMENT ON COLUMN t_healthcare_position.contact_phone               IS '联系电话';
COMMENT ON COLUMN t_healthcare_position.contact_person              IS '联系人';
COMMENT ON COLUMN t_healthcare_position.remark                      IS '备注';
COMMENT ON COLUMN t_healthcare_position.content                     IS '详细说明（支持HTML）';


-- ============================================================
-- 3. 银行/金融系统岗位表 (t_finance_position)
-- ============================================================
CREATE TABLE IF NOT EXISTS t_finance_position (
    id                          BIGINT          PRIMARY KEY,

    -- ==================== 机构信息 ====================
    institution_name            VARCHAR(200)    NOT NULL,
    institution_category        VARCHAR(30)     NOT NULL,
    institution_type            VARCHAR(50),
    institution_logo            VARCHAR(500),
    branch_name                 VARCHAR(200),

    -- ==================== 岗位信息 ====================
    position_name               VARCHAR(200)    NOT NULL,
    position_category           VARCHAR(50),
    recruitment_type            VARCHAR(30)     NOT NULL,

    -- ==================== 地区信息 ====================
    province                    VARCHAR(30),
    city                        VARCHAR(50),
    work_location               VARCHAR(200),
    is_remote                   BOOLEAN         DEFAULT FALSE,

    -- ==================== 报考要求 ====================
    education_requirement       VARCHAR(30),
    degree_requirement          VARCHAR(30),
    major_requirement           VARCHAR(500),
    major_preference            TEXT[]          DEFAULT '{}',
    age_limit                   INTEGER,
    work_experience             VARCHAR(50),
    recruitment_count           INTEGER,

    -- ==================== 金融行业特殊要求 ====================
    cert_requirements           TEXT[]          DEFAULT '{}',
    language_requirement        VARCHAR(100),
    computer_requirement        VARCHAR(100),
    other_requirement           TEXT,

    -- ==================== 薪资待遇 ====================
    salary_min                  INTEGER,
    salary_max                  INTEGER,
    salary_text                 VARCHAR(100),
    benefits                    TEXT,

    -- ==================== 考试信息 ====================
    exam_content                VARCHAR(500),
    exam_time                   TIMESTAMPTZ,
    interview_rounds            VARCHAR(100),

    -- ==================== 报名信息 ====================
    reg_start_date              TIMESTAMPTZ,
    reg_end_date                TIMESTAMPTZ,
    apply_link                  VARCHAR(500),

    -- ==================== 补充信息 ====================
    position_status             VARCHAR(20)     DEFAULT '招聘中',
    contact_info                VARCHAR(200),
    remark                      TEXT,
    content                     TEXT,

    -- ==================== 审计字段 ====================
    sort_order                   INTEGER         DEFAULT 0,
    is_deleted                  BOOLEAN         NOT NULL DEFAULT FALSE,
    created_at                  TIMESTAMPTZ     NOT NULL DEFAULT NOW(),
    updated_at                  TIMESTAMPTZ     NOT NULL DEFAULT NOW(),

    -- ==================== 约束 ====================
    CONSTRAINT chk_fin_inst_category CHECK (
        institution_category IN ('银行', '证券', '保险', '基金', '信托', '期货', '监管机构', '金融科技')
    ),
    CONSTRAINT chk_fin_recruit_type CHECK (
        recruitment_type IN ('秋招', '春招', '社招', '实习', '定向')
    ),
    CONSTRAINT chk_fin_education CHECK (
        education_requirement IS NULL
        OR education_requirement IN ('不限', '大专', '本科', '硕士', '博士')
    ),
    CONSTRAINT chk_fin_age CHECK (
        age_limit IS NULL OR (age_limit >= 18 AND age_limit <= 45)
    ),
    CONSTRAINT chk_fin_recruitment CHECK (
        recruitment_count IS NULL OR recruitment_count > 0
    ),
    CONSTRAINT chk_fin_salary CHECK (
        salary_min IS NULL OR salary_max IS NULL OR salary_min <= salary_max
    ),
    CONSTRAINT chk_fin_status CHECK (
        position_status IS NULL OR position_status IN ('招聘中', '已结束', '即将开始')
    )
);

CREATE INDEX idx_fin_inst_category ON t_finance_position (institution_category) WHERE is_deleted = FALSE;
CREATE INDEX idx_fin_inst_type     ON t_finance_position (institution_type) WHERE is_deleted = FALSE;
CREATE INDEX idx_fin_recruit_type  ON t_finance_position (recruitment_type) WHERE is_deleted = FALSE;
CREATE INDEX idx_fin_pos_category  ON t_finance_position (position_category) WHERE is_deleted = FALSE;
CREATE INDEX idx_fin_location      ON t_finance_position (province, city) WHERE is_deleted = FALSE;
CREATE INDEX idx_fin_composite     ON t_finance_position (institution_category, recruitment_type, province) WHERE is_deleted = FALSE;
CREATE INDEX idx_fin_education     ON t_finance_position (education_requirement) WHERE is_deleted = FALSE;

CREATE INDEX idx_fin_inst_name     ON t_finance_position USING btree (institution_name varchar_pattern_ops) WHERE is_deleted = FALSE;
CREATE INDEX idx_fin_pos_name      ON t_finance_position USING btree (position_name varchar_pattern_ops) WHERE is_deleted = FALSE;
CREATE INDEX idx_fin_salary        ON t_finance_position (salary_min, salary_max) WHERE is_deleted = FALSE;
CREATE INDEX idx_fin_status        ON t_finance_position (position_status) WHERE is_deleted = FALSE;
CREATE INDEX idx_fin_reg_end       ON t_finance_position (reg_end_date ASC NULLS LAST) WHERE is_deleted = FALSE;

CREATE TRIGGER trg_fin_position_updated_at
    BEFORE UPDATE ON t_finance_position
    FOR EACH ROW EXECUTE FUNCTION fn_update_timestamp();

COMMENT ON TABLE  t_finance_position                            IS '银行/金融系统岗位表';
COMMENT ON COLUMN t_finance_position.institution_name           IS '金融机构名称';
COMMENT ON COLUMN t_finance_position.institution_category       IS '机构大类（银行/证券/保险/基金/信托/期货/监管/金融科技）';
COMMENT ON COLUMN t_finance_position.institution_type           IS '机构细分类型';
COMMENT ON COLUMN t_finance_position.institution_logo           IS '机构Logo';
COMMENT ON COLUMN t_finance_position.branch_name                IS '分支机构名称';
COMMENT ON COLUMN t_finance_position.position_name              IS '岗位名称';
COMMENT ON COLUMN t_finance_position.position_category          IS '岗位类别';
COMMENT ON COLUMN t_finance_position.recruitment_type           IS '招聘类型（秋招/春招/社招/实习/定向）';
COMMENT ON COLUMN t_finance_position.province                   IS '省份';
COMMENT ON COLUMN t_finance_position.city                       IS '城市';
COMMENT ON COLUMN t_finance_position.work_location              IS '详细工作地点';
COMMENT ON COLUMN t_finance_position.is_remote                  IS '是否支持远程';
COMMENT ON COLUMN t_finance_position.education_requirement      IS '学历要求';
COMMENT ON COLUMN t_finance_position.degree_requirement         IS '学位要求';
COMMENT ON COLUMN t_finance_position.major_requirement          IS '专业要求';
COMMENT ON COLUMN t_finance_position.major_preference           IS '优先专业列表';
COMMENT ON COLUMN t_finance_position.age_limit                  IS '年龄上限（周岁）';
COMMENT ON COLUMN t_finance_position.work_experience            IS '工作经验要求';
COMMENT ON COLUMN t_finance_position.recruitment_count          IS '招聘人数';
COMMENT ON COLUMN t_finance_position.cert_requirements          IS '证书要求列表';
COMMENT ON COLUMN t_finance_position.language_requirement       IS '语言要求';
COMMENT ON COLUMN t_finance_position.computer_requirement       IS '计算机要求';
COMMENT ON COLUMN t_finance_position.other_requirement          IS '其他要求';
COMMENT ON COLUMN t_finance_position.salary_min                 IS '最低月薪（单位：k）';
COMMENT ON COLUMN t_finance_position.salary_max                 IS '最高月薪（单位：k）';
COMMENT ON COLUMN t_finance_position.salary_text                IS '薪资文本说明';
COMMENT ON COLUMN t_finance_position.benefits                   IS '福利待遇';
COMMENT ON COLUMN t_finance_position.exam_content               IS '考试内容';
COMMENT ON COLUMN t_finance_position.exam_time                  IS '考试时间';
COMMENT ON COLUMN t_finance_position.interview_rounds           IS '面试轮次说明';
COMMENT ON COLUMN t_finance_position.reg_start_date             IS '报名开始';
COMMENT ON COLUMN t_finance_position.reg_end_date               IS '报名截止';
COMMENT ON COLUMN t_finance_position.apply_link                 IS '网申/报名链接';
COMMENT ON COLUMN t_finance_position.position_status            IS '状态';
COMMENT ON COLUMN t_finance_position.contact_info               IS '联系方式';
COMMENT ON COLUMN t_finance_position.remark                     IS '备注';
COMMENT ON COLUMN t_finance_position.content                    IS '详细说明（支持HTML）';

COMMIT;

````


### 已实现的功能：
1. 以实现easyExcel基本配置
2. 以前表的所有的mapper，entity都已实现，在：haifeng-common\src\main\java\com\haifeng\common\entity\employment的下面

### 待实现需求：
admin端实现：分页，修改，详情，禁用，删除
在：haifeng-admin\src\main\java\com\haifeng\admin\controller\下面创建模块：employment，再根据功能创建包，在下面分别创建多个接口
employment
--industryPosition
----FinancePosition
----xxxx
--grassrootPosition
--contentManagement
--civilService

教师招聘
医疗卫生
银行/金融


### 任务1【完成对教师招聘岗位表的分页】
接口1：后端分页展示
实现分页查询t_teacher_position表的：id,school_name,school_type,school_nature,position_name,recruitment_type,province,city,district,position_status

支持模糊查询的字段：school_name,position_name
支持精准查询的字段：school_type,province，city,district,position_status
注意这个模糊查询和精准查询是“and”的关系。

##### 每个分页的旁边：详情，删除，禁用
接口2：查看详情 
接口3：修改
接口4：删除，物理删除
接口5：禁用，设置：is_deleted

##### 新增接口：
接口6：级联物理删除，支持多选id并物理删除
接口7：熟悉爱你导入xlsx的功能，导入所有的字段


### 任务2【完成对医疗卫生招聘岗位表的查询】
接口1：前端分页展示
实现分页查询t_healthcare_position表的id,institution_name,position_name,department,position_category,province,city,district，position_status
支持模糊查询的字段:institution_name，position_name
支持精准查询的字段:institution_nature，department，province，city，district，position_status

##### 每个分页的旁边：详情，删除，禁用
接口2：查看详情
接口3：修改
接口4：删除，物理删除
接口5：禁用，设置：is_deleted

##### 新增接口：
接口6：级联物理删除，支持多选id并物理删除
接口7：熟悉爱你导入xlsx的功能，导入所有的字段




### 任务3【完成对银行/金融系统岗位表的查询】
接口1：前端分页展示（不需要登录）
实现分页查询t_finance_position表的id,institution_name,institution_category,position_name,position_category,recruitment_type,province,city,position_status

支持模糊查询的字段：institution_name，position_name,
支持精准查询的字段：institution_category，institution_type，province，city，position_status


##### 每个分页的旁边：详情，删除，禁用
接口2：查看详情
接口3：修改
接口4：删除，物理删除
接口5：禁用，设置：is_deleted

##### 新增接口：
接口6：级联物理删除，支持多选id并物理删除
接口7：熟悉爱你导入xlsx的功能，导入所有的字段

### 注意事项
1. JSONB字段的在common实现过，没有就不考虑
2. 需要系统操作的注解:package com.haifeng.common.annotation;public @interface OperationLog
3. 不要使用git命令提交，最后修改的统一提交，遇到冲突的需要给我判决，一般都不会遇到。
4. 省份已实现枚举类:D:\exeProject\ideaProjects\Project-HaiFeng\haifeng-common\src\main\java\com\haifeng\common\enums\ProvinceEnum.java
5. 复用现有Excel导入模式
6. 分页需要查询按sort_order和二级的时间倒序排列

### xlsx导入注意事项
1. 使用 EasyExcel 的 @ExcelProperty("院校名称") 注解，来动态绑定xlsx的导入。
2. text[]字段的值必须用逗号格式，支持中英文逗号分隔，意思是逗号分隔，代表是不同的值，可以复用：package com.haifeng.admin.excel.university;StringArrayConverter，然后在 DTO 字段上加上 @ExcelProperty(converter = StringToArrayConverter.class)
3. 可能涉及到类型转换的问题：基础类型（如 Excel 里的 "2026-05-01" 转 Date，"1000" 转 Integer），EasyExcel 和 Spring Boot 默认就能完美转换。
4. 上传过程中遇到了错误，报第几行的具体错误，比如类型错误呀什么的，事务一致性，出错了全部回滚
5. 提供一个接口，先检查用户传入的xlsx是否有语法等错误，先校验，错误处理：明确说清楚是什么报错了，比如哪一行，哪个表头，要明确信息