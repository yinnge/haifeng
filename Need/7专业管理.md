# 总的项目需求

## 数据库表：

### flyway已实现的数据库表：
地址：Project-HaiFeng\haifeng-admin\src\main\resources\db\migration

#### 实现好的
1. admin_logs
2. sys_role
3. sys_module
4. sys_role_module
5. sys_admin
6. t_member
7. 会员订单表 (member_orders)
8. 推荐佣金表 (t_referral_commission)
9. 消息通知表 (t_member_notification)
10. 系统设置表 (system_settings)
11. 公告表 (t_announcements)
12. 规划师表 (t_planners)
13. 培训机构表 (t_institutions)
14. 院校表 (universities)
15. 院校详情表 (universities_detail)
16. 校园图册表（t_campus_gallery）
17. 院校适应指南表 (university_guides)
18. 实验室表 (laboratories)
19. 院系主表 (t_department)
20. 院系分析报告表 (department_reports)
21. 学科评估表 (t_subject_evaluation)


### 本次新增的表：
1. 专业表 (t_major)
2. 专业详情表（t_major_detail）
3. 考研专业表 (t_postgrad_major)
4. 考研专业-大学 关联表 (t_postgrad_major_university)

#### 原始表
位置：Project-HaiFeng\haifeng-admin\src\main\resources\db\migration\table\DataTable7.md

### 需求：
身份限制： 管理员
后台管理： 专业管理[父模块]--专业列表[子模块]--专业详情[子模块]--考研专业[子模块]--考研专业关联[子模块]

### 本次需要实现的：
专业列表[子模块]--专业详情[子模块]--考研专业[子模块]

### 待实现需求：
**仅限管理员身份**

1. 分页展示专业表，右边三个按钮：详情、禁用、删除
2. 点击查看详情，tabs展示专业和专业详情表，事务一致性保证数据一致
3. 实现一个功能：支持导入xlsx，实现了批量导入数据的功能，后续会详细介绍
4. 实现分页查询考研专业表 (t_postgrad_major),支持增删改查,右边有删除，查看详情按钮（查和修改），禁用。
5. 实现一个导入xlsx批量导入数据库的功能，需要实现两个传入xlsx的接口，对应两个数据库表
6. 分页展示：考研专业-大学 关联表 (t_postgrad_major_university)
7. 实现传入xlsx接口，实现批量导入数据

### 分页注意

1. 实现批量删除的功能，注意：这是软删除
2. 支持两个删除，一个是软删除，另一个是硬删除，在列表旁边即可，
3. 删除的可以恢复，（也就是status可以变为1或is_deleted = false），否则数据库会越来越多，新建一个新的还外键和主键在而建不了
4. 所有列表右边都有三个按钮：1.删除（硬删除） 2.禁用（软删除，重新点击后恢复） 3. 详情（点击可以查看所有和修改）

### 任务1【完成数据表】
根据db\migration\table\DataTable6.md的数据库表，创建并完善V7__create_major_tables.sql,放到flyway的迁移下面，要注意索引，注释等规范

### 任务2【完成专业表 (t_major)】
1. 列表展示：major_code,major_name,discipline_name，major_type，major_category，status，右边有详情，删除，禁用
2. 点击详情，展示专业表和专业详情表的所有字段（tabs分开，他们是一对一的关系）
3. 实现xlsx批量导入功能，两个xlsx
4. 模糊查询：major_code，major_name，discipline_name，major_type，major_category，status


### 任务3【考研专业表 (t_postgrad_major)】

1. 展示列表：major_name，major_code，degree_type，discipline_category，popularity，difficulty，status。支持修改，禁用，删除
2. 点击详情查看所有字段，支持修改
3. 实现一个xlsx文件上传功能，上传成功后，将数据导入数据库
4. 实现模糊查询：major_name，major_code，degree_type，discipline_category，popularity，difficulty，status

### 任务4【考研专业-大学 关联表 (t_postgrad_major_university)】

1. 列表展示：university_name，postgrad_major_name，sort_order，is_deleted
2. 新增支持xlsx导入功能，导入所有字段
3. 实现模糊查询：is_deleted，university_name，postgrad_major_name


### 任务5【完成接口和功能概述文档】
创建接口和功能概述文档，命名为：order7.md,放到：Project-HaiFeng\Products，这个下面，最开始也就是开头要写清楚，4个xlsx的表头（EasyExcel定义的）准确名称（包含sheet的,没有JSONB不需要）


### XLSX注意事项：

1. 一个是主表，其他的都是外键，有一对多的，有1对1的，1对1的不能使得外键或者能代替外键的重复，
2. 有外键的xlsx都不会传外键id，而是能代替外键唯一的字段，比如大学名，城市名，需要联表找出id并传入
3. 关于如何识别哪一列跟数据库相对的问题，我认为处理方案是按名称，使用 EasyExcel 的 @ExcelProperty("院校名称") 注解即可，针对这次的名称，结合注释等信息自己决定就可以，后续列出来即可。
4. text[]字段的值必须用逗号格式，支持中英文逗号分隔，意思是逗号分隔，代表是不同的值
````
可以写一个自定义的 Converter：

Java
// 伪代码示例
public class StringToArrayConverter implements Converter<String[]> {
@Override
public String[] convertToJavaData(ReadCellData<?> cellData, ...) {
String val = cellData.getStringValue();
if (val == null) return null;
// 用正则同时兼容中英文逗号
return val.split("[,，]");
}
}
然后在 DTO 字段上加上 @ExcelProperty(converter = StringToArrayConverter.class) 即可。
````
5. 可能涉及到类型转换的问题：基础类型（如 Excel 里的 "2026-05-01" 转 Date，"1000" 转 Integer），EasyExcel 和 Spring Boot 默认就能完美转换。
6. 上传过程中遇到了错误，报第几行的具体错误，比如类型错误呀什么的
7. 使用事务保证一致性
8. 如果内容遇到了空值就默认是null，让数据库判断去吧。意思就是除了数据库指定不能为空之外，其他都可以为空
9. 正确顺序：传入的数据表，根据接口不同，有的是主表，有的不是主表
- 主表：检查表头规范性（比如检查看是否有超过规定的表头），检查unique字段的这一列是否有重复的,正确的话就传入数据
- 外键表：先检查院校名（1对1的不能重复，外键的必须在主表里面有对应的）不能重复检查外键表是否有数据，没有数据就报错，有数据就继续
- 错误处理：明确说清楚是什么报错了，比如哪一行，哪个表头，要明确信息
- 最佳实践：这就需要用到 Spring 的事务一致性。
````
在解析 Excel 的过程中，如果有校验不通过的（比如“第5行：院校名称已存在”），把这些错误信息收集到一个 List<String> errorMsgs 里。
如果 errorMsgs 不为空，直接抛出一个自定义异常 throw new BusinessException(String.join(";", errorMsgs))，并中断解析。
只要你的 Service 方法上加了 @Transactional(rollbackFor = Exception.class)，抛出异常后，前面插入的几行数据会自动全部回滚。
````
10. JSONB需要在原表的的基础上新建sheet1，sheet2等：Sheet 0：基础主表 (必须存在)，Sheet 1：附属labs_jsonb 字段（需要有逻辑外键）


### 功能统一需求

1. 统一用雪花算法
2. 统一使用status进行软删除处理
3. 按子模块独立Controller，每个子模块独立Controller，复用现有Excel导入模式



### 这是院校主表，很多表的外键：

-- 院校主表
CREATE TABLE t_universities (
id                  BIGINT        PRIMARY KEY,
name                VARCHAR(50)   NOT NULL,
name_en             VARCHAR(50)   NOT NULL,
province_name       VARCHAR(50)   NOT NULL,
city_name           VARCHAR(50)   NOT NULL,
region              VARCHAR(50)   NOT NULL,
category            VARCHAR(50)   NOT NULL,
major_count         INTEGER       DEFAULT 0,
education_level     VARCHAR(50),
nature              VARCHAR(50),
recommendation_rate DECIMAL(5,2),
recommendation_year INTEGER,
has_doctorate       BOOLEAN       DEFAULT false,
has_master          BOOLEAN       DEFAULT false,
department          VARCHAR(100),
tags                TEXT[],
famous_union        VARCHAR(50),
image_url           VARCHAR(500),
introduction        TEXT,
sort_order          INTEGER       DEFAULT 0,
status              SMALLINT      DEFAULT 1 NOT NULL,
created_at          TIMESTAMPTZ   DEFAULT CURRENT_TIMESTAMP NOT NULL,
updated_at          TIMESTAMPTZ   DEFAULT CURRENT_TIMESTAMP NOT NULL
);