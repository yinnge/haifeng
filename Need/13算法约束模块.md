# 总的项目需求

## 数据库表：

### flyway已实现的数据库表：
地址：Project-HaiFeng\haifeng-admin\src\main\resources\db\migration

#### 原始表
位置：Project-HaiFeng\haifeng-admin\src\main\resources\db\migration\table\DataTable13.md

#### 需要用到的表
### 1.4 `t_member_gaokao` 高考档案表 —【全新创建】

这是**最核心的业务表**，存储用户的高考成绩 + 选科 + 身体条件 + 所有约束项：


```
-- ============================================================
-- 用户高考档案表 (t_member_gaokao)
-- 描述：一个用户一条记录，存储高考的所有业务信息
--       系统推荐算法的核心输入数据
-- ============================================================

CREATE TABLE IF NOT EXISTS t_member_gaokao (

    id                      SERIAL          PRIMARY KEY,
    member_id               INTEGER         NOT NULL UNIQUE,    -- 关联 t_member（一对一）

    -- ============================================================
    -- 一、高考基本信息（必填，推荐算法核心输入）
    -- ============================================================
    gaokao_year             SMALLINT,                           -- 高考年份（2025/2026）
    gaokao_province         VARCHAR(30),                        -- 高考省份（四川/广东...）
    score                   INTEGER,                            -- 高考总分
    rank                    INTEGER,                            -- 位次（可自动计算）

    -- ============================================================
    -- 二、选科信息
    -- ============================================================
    -- 首选科目（3+1+2 的"1"）
    subject_type            VARCHAR(20),                        -- 第一科目
    second_subject_type     VARCHAR(20),                        -- 第二科目
    third_subject_type      VARCHAR(20),                        -- 第三科目

    -- ============================================================
    -- 三、各科成绩（用于匹配单科分数要求）
    -- ============================================================
    score_chinese           INTEGER,                            -- 语文
    score_math              INTEGER,                            -- 数学
    score_english           INTEGER,                            -- 外语
    score_subject_1         INTEGER,                            -- 首选科目分数（物理/历史原始分）
    score_subject_2         INTEGER,                            -- 再选科目1分数（赋分后）
    score_subject_3         INTEGER,                            -- 再选科目2分数（赋分后）

    -- ============================================================
    -- 四、外语语种
    -- ============================================================
    foreign_language        VARCHAR(20)     DEFAULT '英语',      -- 英语/日语/俄语/德语/法语/西班牙语/其他

    -- ============================================================
    -- 五、身体视觉条件
    -- ============================================================
    is_color_blind          BOOLEAN         DEFAULT FALSE,       -- 色盲
    is_color_weak           BOOLEAN         DEFAULT FALSE,       -- 色弱
    vision_left             NUMERIC(3,1),                        -- 左眼裸眼视力（如 4.8）
    vision_right            NUMERIC(3,1),                        -- 右眼裸眼视力
    has_smell_disorder      BOOLEAN         DEFAULT FALSE,       -- 嗅觉迟钝

    -- ============================================================
    -- 六、身体指标
    -- ============================================================
    height_cm               INTEGER,                             -- 身高（厘米）
    weight_kg               NUMERIC(5,1),                        -- 体重（公斤）
    is_left_handed          BOOLEAN         DEFAULT FALSE,       -- 左利手
    has_tattoo              BOOLEAN         DEFAULT FALSE,       -- 纹身
    has_scar                BOOLEAN         DEFAULT FALSE,       -- 面部明显疤痕
    has_stutter             BOOLEAN         DEFAULT FALSE,       -- 口吃

    -- ============================================================
    -- 七、身份条件
    -- ============================================================
    is_fresh_graduate       BOOLEAN         DEFAULT TRUE,        -- 应届生（TRUE=应届，FALSE=往届/复读）
    political_status        VARCHAR(20)     DEFAULT '群众',      -- 政治面貌（群众/共青团员/中共党员）
    household_type          VARCHAR(20),                         -- 户籍类型（城镇/农村）
    is_poverty_county       BOOLEAN         DEFAULT FALSE,       -- 是否国家级贫困县户籍

    -- ============================================================
    -- 八、计算字段（系统自动填充）
    -- ============================================================
    batch_line_score        INTEGER,                             -- 所在批次的省控线（系统查表填入）
    score_above_line        INTEGER,                             -- 线差 = score - batch_line_score

    -- ============================================================
    -- 审计字段
    -- ============================================================
    created_at              TIMESTAMPTZ     NOT NULL DEFAULT NOW(),
    updated_at              TIMESTAMPTZ     NOT NULL DEFAULT NOW()
);


-- ----------------------------------------------------------
-- 索引
-- ----------------------------------------------------------
CREATE INDEX idx_mg_member ON t_member_gaokao (member_id);
CREATE INDEX idx_mg_province_year ON t_member_gaokao (gaokao_province, gaokao_year);
CREATE INDEX idx_mg_score ON t_member_gaokao (score DESC NULLS LAST);


-- ----------------------------------------------------------
-- 触发器：自动更新 updated_at
-- ----------------------------------------------------------
CREATE TRIGGER trg_mg_updated_at
    BEFORE UPDATE ON t_member_gaokao
    FOR EACH ROW EXECUTE FUNCTION fn_update_timestamp();
```
### 专业表 (t_major)
#### major-search.html（专业粗略展示）

```
-- ============================================================
-- 专业信息表 (t_major)
-- 描述：存储高校专业的基本信息、分类体系及就业相关指标数据
-- 作者：DBA
-- 创建日期：2025-01-01
-- ============================================================


-- ----------------------------------------------------------
-- 2. 创建专业信息主表
-- ----------------------------------------------------------
CREATE TABLE IF NOT EXISTS t_major (

    -- ==================== 基础标识 ====================
    id                  BIGSERIAL       PRIMARY KEY,
    major_code          VARCHAR(20)     NOT NULL,           -- 专业代码（教育部国标代码，如 080901）
    major_name          VARCHAR(100)    NOT NULL,           -- 专业名称（如 计算机科学与技术）
    
    discipline_name     VARCHAR(100),                       -- 学科名称
    
    -- ==================== 分类体系 ====================
    major_type          VARCHAR(30)     NOT NULL,           -- 专业类型（本科 / 专科 ）
    major_category      VARCHAR(50),                        -- 专业类别·学科门类（如 工学、农学、理学）
    parent_category     VARCHAR(50),                       -- 所属类别·专业类（如 计算机类、电子信息类）
    major_tags          VARCHAR(50),                       -- 专业标签（如 {"热门","新工科","国家特色"}）
    degree_awarded      VARCHAR(50),                        -- 授予学位（如 工学学士、理学学士）
    study_duration      VARCHAR(20),                        -- 学制（如 四年、五年、4-5年）
                            
    -- ==================== 就业与评价指标 ====================
    employment_rate     NUMERIC(5, 2),                      -- 就业率（%），如 92.50 表示 92.50%
    salary_min          INTEGER,                            -- 薪资范围下限（元/月）
    salary_max          INTEGER,                            -- 薪资范围上限（元/月）
    
    -- ==================== 详细描述 ====================
    description         TEXT,                               -- 专业描述（培养目标、核心课程等）

    -- ==================== 审计字段 ====================
    is_deleted          BOOLEAN         NOT NULL DEFAULT FALSE,   -- 软删除标记
    created_at          TIMESTAMPTZ     NOT NULL DEFAULT NOW(),   -- 创建时间
    updated_at          TIMESTAMPTZ     NOT NULL DEFAULT NOW(),   -- 最后更新时间

    -- ==================== 约束 ====================
    -- 专业代码唯一（同一专业不可重复录入）
    CONSTRAINT uk_major_code
        UNIQUE (major_code),

    -- 就业率范围：0% ~ 100%
    CONSTRAINT chk_employment_rate
        CHECK (employment_rate IS NULL OR (employment_rate >= 0 AND employment_rate <= 100)),

    -- 薪资逻辑：下限 ≤ 上限，且不为负
    CONSTRAINT chk_salary_range
        CHECK (
            (salary_min IS NULL AND salary_max IS NULL)
            OR (salary_min IS NULL)
            OR (salary_max IS NULL)
            OR (salary_min >= 0 AND salary_max >= 0 AND salary_min <= salary_max)
        )
    
);


```

#### 新增表

1. 约束条件字典表 (t_constraint_dict)
2. 专业约束关联表 (t_major_constraint)
3. 安全系数等级字典 (t_safety_level_dict)

#### 实现模块
算法约束模块[父模块]--约束字典[子模块]--专业约束关联[子模块]--安全系数[子模块]

### 待实现需求：
**仅限管理员身份**

1. 分页展示：约束条件字典表 (t_constraint_dict)，详情、禁止、删除   【用户高考档案表 (t_member_gaokao)里面的字段做映射，但那个表还没实现任何功能，所以，你只需要知道这个表的字段】
2. 专业约束关联表 (t_major_constraint)，详情、禁止、删除 【专业实体类已实现】
3. 安全系数等级字典 (t_safety_level_dict)，详情、删除【硬删除】

### 分页注意

1. 模块实现批量删除的功能，注意：这是硬删除
2. 所有列表右边都有两个按钮：1.删除（硬删除） 2. 详情（点击可以查看所有和修改）

### 任务1【完成数据表】
根据db\migration\table\DataTable13.md的数据库表，创建并完善V13__algorithm__settings.sql,放到flyway的迁移下面，要注意索引，注释等规范

### 任务2【完成约束条件字典表 (t_constraint_dict)】

1. 列表展示：code,category，severity，check_field，is_active右边有详情，禁用，删除[硬删除]
2. 点击详情，展示所有字段，支持修改
3. 无模糊查询
4. check_field（对应 t_member_gaokao 表的哪个字段）：只能是高考档案表里面的字段，从：二、选科信息到 七、身份条件
5. 目前只实现增删改查，不做数据表对应的代码实现约束

### 任务3【完成专业约束关联表 (t_major_constraint)】

1. 列表展示：major_code,major_name,constraint_code,constraint_name.右边有详情和删除
2. 精准查询：major_code,major_name,constraint_code,constraint_name
3. 支持xlsx导入功能，一个导入xlsx的接口，导入除了created_at的
4. 传入xlsx只有major_name和constraint_name这两个字段，映射找到major_code和constraint_code
5. 传入xlsx先判断major_name和constraint_name能否在对应外键数据库找到，然后有一个检查的按钮
6. 上传xlsx功能，遇到错误，之前传的无效


### 任务4【完成安全系数等级字典(t_safety_level_dict)】

1. 列表展示：level，code，name，name_short，min_coefficient，max_coefficient，confidence。右边有详情和删除
2. 不需要精准查询
3. 不需要xlsx导入功能
4. 目前只实现增删改查，不做数据表对应的具体逻辑代码实现约束


### 功能统一需求

1. 统一用雪花算法
2. 按子模块独立Controller，每个子模块独立Controller，复用现有Excel导入模式

### 总结
1. 简单的增删改查，分页展示

### XLSX注意事项：

1. 一个是主表，其他的都是外键，有一对多的，有1对1的，1对1的不能使得外键或者能代替外键的重复，
2. 有外键的xlsx都不会传外键id，而是能代替外键唯一的字段，比如大学名，城市名，需要联表找出id并传入（先检查name对应的外键是否存在）
3. 关于如何识别哪一列跟数据库相对的问题，我认为处理方案是按名称，使用 EasyExcel 的 @ExcelProperty("院校名称") 注解即可，针对这次的名称，结合注释等信息自己决定就可以，后续列出来即可。
4. text[]字段的值必须用逗号格式，支持中英文逗号分隔，意思是逗号分隔，代表是不同的值
````
可以写一个自定义的 Converter：

Java
// 伪代码示例
public class StringToArrayConverter implements Converter<String[]> {
@Override
public String[] convertToJavaData(ReadCellData<?> cellData, ...) {
String val = cellData.getStringValue();
if (val == null) return null;
// 用正则同时兼容中英文逗号
return val.split("[,，]");
}
}
然后在 DTO 字段上加上 @ExcelProperty(converter = StringToArrayConverter.class) 即可。
````
5. 可能涉及到类型转换的问题：基础类型（如 Excel 里的 "2026-05-01" 转 Date，"1000" 转 Integer），EasyExcel 和 Spring Boot 默认就能完美转换。
6. 上传过程中遇到了错误，报第几行的具体错误，比如类型错误呀什么的
7. 使用事务保证一致性
8. 如果内容遇到了空值就默认是null，让数据库判断去吧。意思就是除了数据库指定不能为空之外，其他都可以为空
9. 正确顺序：传入的数据表，根据接口不同，有的是主表，有的不是主表
- 主表：检查表头规范性（比如检查看是否有超过规定的表头），检查unique字段的这一列是否有重复的,正确的话就传入数据
- 外键表：先检查院校名（1对1的不能重复，外键的必须在主表里面有对应的）不能重复检查外键表是否有数据，没有数据就报错，有数据就继续
- 错误处理：明确说清楚是什么报错了，比如哪一行，哪个表头，要明确信息
- 最佳实践：这就需要用到 Spring 的事务一致性。
````
在解析 Excel 的过程中，如果有校验不通过的（比如“第5行：院校名称已存在”），把这些错误信息收集到一个 List<String> errorMsgs 里。
如果 errorMsgs 不为空，直接抛出一个自定义异常 throw new BusinessException(String.join(";", errorMsgs))，并中断解析。
只要你的 Service 方法上加了 @Transactional(rollbackFor = Exception.class)，抛出异常后，前面插入的几行数据会自动全部回滚。
````
10. JSONB需要在原表的的基础上新建sheet1，sheet2等：Sheet 0：基础主表 (必须存在)，Sheet 1：附属labs_jsonb 字段（需要有逻辑外键）
11. xlsx的传入功能之前已实现过，可以借鉴
12. Excel导入时遇到重复数据应该如何处理？ 拒绝导入并报错，说明具体是什么错误，哪一行，哪一列
