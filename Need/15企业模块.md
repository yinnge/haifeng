# 总的项目需求

## 数据库表：

### flyway已实现的数据库表：
地址：Project-HaiFeng\haifeng-admin\src\main\resources\db\migration

#### 原始表
位置：Project-HaiFeng\haifeng-admin\src\main\resources\db\migration\table\DataTable15.md

#### 需要用到的表
 **一、行业表**

```
-- ============================================================
-- 行业表 (t_industry)
-- 描述：行业基本信息，用于列表展示、筛选
-- ============================================================

BEGIN;

CREATE TABLE IF NOT EXISTS t_industry (

    id                      SERIAL          PRIMARY KEY,
    industry_name           VARCHAR(100)    NOT NULL,           -- 行业名称
    category                VARCHAR(50),                        -- 行业分类（如：信息技术、金融、制造业）
    icon_class              VARCHAR(100),                       -- 图标类名（如：fa-solid fa-microchip）
    description             TEXT,                               -- 行业描述

    -- ==================== 核心指标 ====================
    annual_growth_rate      NUMERIC(5, 2),                      -- 年增长率（%）
    market_scale            VARCHAR(50),                        -- 市场规模（如：1.8万亿）
    talent_gap              VARCHAR(50),                        -- 人才缺口（如：120万）
    investment_heat         NUMERIC(5, 2),                      -- 投资热度（%）

    -- ==================== 趋势（涨跌方向） ====================
    growth_trend            VARCHAR(10),                        -- 增长趋势
    market_trend            VARCHAR(10),                        -- 市场趋势
    talent_trend            VARCHAR(10),                        -- 人才趋势
    investment_trend        VARCHAR(10),                        -- 投资趋势

    -- ==================== 审计字段 ====================
    is_deleted              BOOLEAN         NOT NULL DEFAULT FALSE,
    created_at              TIMESTAMPTZ     NOT NULL DEFAULT NOW(),
    updated_at              TIMESTAMPTZ     NOT NULL DEFAULT NOW(),

    -- ==================== 约束 ====================
    CONSTRAINT uk_industry_name UNIQUE (industry_name),

    CONSTRAINT chk_growth_trend     CHECK (growth_trend     IS NULL OR growth_trend     IN ('上升','稳定','下降')),
    CONSTRAINT chk_market_trend     CHECK (market_trend     IS NULL OR market_trend     IN ('上升','稳定','下降')),
    CONSTRAINT chk_talent_trend     CHECK (talent_trend     IS NULL OR talent_trend     IN ('上升','稳定','下降')),
    CONSTRAINT chk_investment_trend CHECK (investment_trend  IS NULL OR investment_trend  IN ('上升','稳定','下降')),

    CONSTRAINT chk_annual_growth    CHECK (annual_growth_rate IS NULL OR annual_growth_rate BETWEEN -100 AND 1000),
    CONSTRAINT chk_investment_heat  CHECK (investment_heat    IS NULL OR investment_heat    BETWEEN 0 AND 100)
);


-- 索引
CREATE INDEX idx_industry_category
    ON t_industry (category) WHERE is_deleted = FALSE;

CREATE INDEX idx_industry_growth
    ON t_industry (annual_growth_rate DESC NULLS LAST) WHERE is_deleted = FALSE;

CREATE INDEX idx_industry_name_search
    ON t_industry USING btree (industry_name varchar_pattern_ops) WHERE is_deleted = FALSE;


-- 触发器
CREATE TRIGGER trg_industry_updated_at
    BEFORE UPDATE ON t_industry
    FOR EACH ROW EXECUTE FUNCTION fn_update_timestamp();


-- 注释
COMMENT ON TABLE  t_industry                       IS '行业基本信息表';
COMMENT ON COLUMN t_industry.industry_name         IS '行业名称';
COMMENT ON COLUMN t_industry.category              IS '行业分类（如：信息技术、金融、制造业）';
COMMENT ON COLUMN t_industry.icon_class            IS '图标CSS类名（Font Awesome，如：fa-solid fa-microchip）';
COMMENT ON COLUMN t_industry.description           IS '行业描述';
COMMENT ON COLUMN t_industry.annual_growth_rate    IS '年增长率（%）';
COMMENT ON COLUMN t_industry.market_scale          IS '市场规模（如：1.8万亿）';
COMMENT ON COLUMN t_industry.talent_gap            IS '人才缺口（如：120万）';
COMMENT ON COLUMN t_industry.investment_heat       IS '投资热度（%）';
COMMENT ON COLUMN t_industry.growth_trend          IS '增长趋势（上升/稳定/下降）';
COMMENT ON COLUMN t_industry.market_trend          IS '市场趋势（上升/稳定/下降）';
COMMENT ON COLUMN t_industry.talent_trend          IS '人才趋势（上升/稳定/下降）';
COMMENT ON COLUMN t_industry.investment_trend      IS '投资趋势（上升/稳定/下降）';

COMMIT;
```


#### 新增表

1. 企业表 (t_enterprise)
2. 企业岗位表 (t_enterprise_position)
3. 企业-行业关联表 (t_enterprise_industry)

#### 实现模块
15. 企业管理[父模块]--企业列表[子模块]--企业行业关联[子模块]

### 待实现需求：
**仅限管理员身份**

1. 分页展示：企业表，详情、禁止、删除   【用户高考档案表 (t_member_gaokao)里面的字段做映射，但那个表还没实现任何功能，所以，你只需要知道这个表的字段】
2. 点击企业的详情，跳转新的页面，展示企业表和企业岗位表，tabs分开
3. 支持导入xlsx批量上传
4. 分页展示企业-行业关联表 (t_enterprise_industry)，详情、删除【硬删除】
5. 支持导入xlsx批量上传

### 分页注意

1. 模块实现批量删除的功能，注意：这是硬删除
2. 所有列表右边都有两个按钮：1.删除（硬删除） 2. 详情（点击可以查看所有和修改）

### 任务1【完成数据表】
根据db\migration\table\DataTable16.md的数据库表，创建并完善V16__enterprise__tables.sql,放到flyway的迁移下面，要注意索引，注释等规范

### 任务2【完成企业表 (t_enterprise)】

1. 列表展示：city_name,enterprise_name，enterprise_nature，enterprise_type，recruitment_status，is_deleted。右边有详情，禁用，删除[硬删除]
2. 点击详情，展示所有字段，支持修改，注意tabs分割字段
3. 模糊/精准查询：city_name，enterprise_name，enterprise_nature，enterprise_type，recruitment_status，is_deleted
4. 支持导入xlsx表，需要一个接口，同时上传企业表 (t_enterprise)和企业岗位表 (t_enterprise_position)


### 任务3【完成企业-行业关联表 (t_enterprise_industry)】

1. 列表展示：enterprise_name,industry_name,constraint_code,constraint_name.右边有详情和删除
2. 精准/模糊查询：enterprise_name,industry_name,constraint_code,constraint_name
3. 支持xlsx导入功能，一个导入xlsx的接口
4. 传入xlsx只有enterprise_name和industry_name这两个字段，联表查询找到enterprise_id和industry_id
5. 传入xlsx先判断major_name和constraint_name能否在对应外键数据库找到，然后有一个检查的按钮
6. 上传xlsx功能，遇到错误，之前传的无效


### 功能统一需求

1. 统一用雪花算法
2. 按子模块独立Controller，每个子模块独立Controller，复用现有Excel导入模式

### 总结
1. 简单的增删改查，分页展示

### XLSX注意事项：

1. 一个是主表，其他的都是外键，有一对多的，有1对1的，1对1的不能使得外键或者能代替外键的重复，
2. 有外键的xlsx都不会传外键id，而是能代替外键唯一的字段，比如大学名，城市名，需要联表找出id并传入（先检查name对应的外键是否存在）
3. 关于如何识别哪一列跟数据库相对的问题，我认为处理方案是按名称，使用 EasyExcel 的 @ExcelProperty("院校名称") 注解即可，针对这次的名称，结合注释等信息自己决定就可以，后续列出来即可。
4. text[]字段的值必须用逗号格式，支持中英文逗号分隔，意思是逗号分隔，代表是不同的值
````
可以写一个自定义的 Converter：

Java
// 伪代码示例
public class StringToArrayConverter implements Converter<String[]> {
@Override
public String[] convertToJavaData(ReadCellData<?> cellData, ...) {
String val = cellData.getStringValue();
if (val == null) return null;
// 用正则同时兼容中英文逗号
return val.split("[,，]");
}
}
然后在 DTO 字段上加上 @ExcelProperty(converter = StringToArrayConverter.class) 即可。
````
5. 可能涉及到类型转换的问题：基础类型（如 Excel 里的 "2026-05-01" 转 Date，"1000" 转 Integer），EasyExcel 和 Spring Boot 默认就能完美转换。
6. 上传过程中遇到了错误，报第几行的具体错误，比如类型错误呀什么的
7. 使用事务保证一致性
8. 如果内容遇到了空值就默认是null，让数据库判断去吧。意思就是除了数据库指定不能为空之外，其他都可以为空
9. 正确顺序：传入的数据表，根据接口不同，有的是主表，有的不是主表
- 主表：检查表头规范性（比如检查看是否有超过规定的表头），检查unique字段的这一列是否有重复的,正确的话就传入数据
- 外键表：先检查院校名（1对1的不能重复，外键的必须在主表里面有对应的）不能重复检查外键表是否有数据，没有数据就报错，有数据就继续
- 错误处理：明确说清楚是什么报错了，比如哪一行，哪个表头，要明确信息
- 最佳实践：这就需要用到 Spring 的事务一致性。
````
在解析 Excel 的过程中，如果有校验不通过的（比如“第5行：院校名称已存在”），把这些错误信息收集到一个 List<String> errorMsgs 里。
如果 errorMsgs 不为空，直接抛出一个自定义异常 throw new BusinessException(String.join(";", errorMsgs))，并中断解析。
只要你的 Service 方法上加了 @Transactional(rollbackFor = Exception.class)，抛出异常后，前面插入的几行数据会自动全部回滚。
````
10. JSONB需要在原表的的基础上新建sheet1，sheet2等：Sheet 0：基础主表 (必须存在)，Sheet 1：附属labs_jsonb 字段（需要有逻辑外键）
11. xlsx的传入功能之前已实现过，可以借鉴
12. Excel导入时遇到重复数据应该如何处理？ 拒绝导入并报错，说明具体是什么错误，哪一行，哪一列
