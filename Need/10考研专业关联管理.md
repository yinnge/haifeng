# 总的项目需求

## 数据库表：

### flyway已实现的数据库表：
地址：Project-HaiFeng\haifeng-admin\src\main\resources\db\migration

#### 实现好的
1. admin_logs
2. sys_role
3. sys_module
4. sys_role_module
5. sys_admin
6. t_member
7. 会员订单表 (member_orders)
8. 推荐佣金表 (t_referral_commission)
9. 消息通知表 (t_member_notification)
10. 系统设置表 (system_settings)
11. 公告表 (t_announcements)
12. 规划师表 (t_planners)
13. 培训机构表 (t_institutions)
14. 院校表 (universities)
15. 院校详情表 (universities_detail)
16. 校园图册表（t_campus_gallery）
17. 院校适应指南表 (university_guides)
18. 实验室表 (laboratories)
19. 院系主表 (t_department)
20. 院系分析报告表 (department_reports)
21. 学科评估表 (t_subject_evaluation)
22. 专业表 (t_major)
23. 专业详情表（t_major_detail）
24. 考研专业表 (t_postgrad_major)
25. 城市表 (t_city)
26. 城市详情表 (t_city_detail)
27. 行业表 (t_industry)
28. 行业详情表 (t_industry_detail)
29. 资源表 (t_resource)
30. 证书表 (t_certificate)
31. 竞赛表 (t_competition)
32. 竞赛详情表 (t_competition_detail)
33. 竞赛-专业 关联表 (t_competition_major)

#### 原始表
位置：Project-HaiFeng\haifeng-admin\src\main\resources\db\migration\table\DataTable11.md

#### 新增表
1. 本科专业-考研方向 关联表 (t_major_postgrad_direction)

用到之前的表：
-- 1. 专业主表 (t_major)
-- ===========================================================
CREATE TABLE t_major (
id                  BIGINT          PRIMARY KEY,
major_code          VARCHAR(20)     NOT NULL,
major_name          VARCHAR(100)    NOT NULL,
discipline_name     VARCHAR(100),
major_type          VARCHAR(30)     NOT NULL,
major_category      VARCHAR(50),
parent_category     VARCHAR(50),
major_tags          VARCHAR(50),
degree_awarded      VARCHAR(50),
study_duration      VARCHAR(20),
employment_rate     NUMERIC(5, 2),
salary_min          INTEGER,
salary_max          INTEGER,
description         TEXT,
status              SMALLINT        NOT NULL DEFAULT 1,
created_at          TIMESTAMPTZ     NOT NULL DEFAULT NOW(),
updated_at          TIMESTAMPTZ     NOT NULL DEFAULT NOW(),

    -- 约束
    CONSTRAINT uk_major_code UNIQUE (major_code, major_name)

# 考研专业表 (t_postgrad_major)

-- ============================================================
-- 考研专业表 (t_postgrad_major)
-- 描述：研究生招生专业目录，包含考试科目、跨考信息、招生院校等
-- ============================================================


CREATE TABLE IF NOT EXISTS t_postgrad_major (

    -- ==================== 基础标识 ====================
    id                      SERIAL          PRIMARY KEY,
    major_name              VARCHAR(100)    NOT NULL,           -- 专业名称（如：金融学）
    major_code              VARCHAR(20)     NOT NULL,           -- 专业代码（如：020204）
    -- ==================== 分类信息 ====================
    degree_type             VARCHAR(20)     NOT NULL,           -- 学位类型（学术学位 / 专业学位）
    discipline_category     VARCHAR(50)     NOT NULL,           -- 学科门类（如：经济学、工学）
    -- ==================== 热度与难度 ====================
    popularity              VARCHAR(10),                        -- 热度（热门 / 一般 / 冷门）
    difficulty              VARCHAR(10),                        -- 难度（高 / 中 / 低）
    -- ==================== 描述信息 ====================
    brief                   TEXT,                               -- 专业简介（简短概括）
    introduction            TEXT,                               -- 专业介绍（详细内容）
    
    -- ==================== 考试与报考 ====================
    exam_subjects           TEXT[]          DEFAULT '{}',        -- 考试科目
    admission_requirements  TEXT[]          DEFAULT '{}',        -- 报考要求

    -- ==================== 跨考信息 ====================
    cross_exam_difficulty   VARCHAR(10),                        -- 跨考难度（较易 / 中等 / 较难）
    cross_exam_description  TEXT,                               -- 跨考难度描述
    
    cross_exam_factors      TEXT[]          DEFAULT '{}',        -- 跨考影响因素
    -- ==================== 审计字段 ====================
    is_deleted              BOOLEAN         NOT NULL DEFAULT FALSE,
    created_at              TIMESTAMPTZ     NOT NULL DEFAULT NOW(),
    updated_at              TIMESTAMPTZ     NOT NULL DEFAULT NOW(),

    -- ==================== 约束 ====================
    -- 专业代码唯一
    CONSTRAINT uk_postgrad_major_code
        UNIQUE (major_code),

#### 待实现模块

1. 专业管理[父模块]--专业列表[子模块]--专业详情[子模块]--考研专业[子模块]--专业考研关联[子模块]
待实现：专业考研关联[子模块]


### 待实现需求：
**仅限管理员身份**

1. 分页展示本科专业-考研方向 关联表 (t_major_postgrad_direction)，支持增删改查,右边有删除（硬删除），查看详情按钮（查和修改）。
2. 实现一个导入xlsx批量导入数据库的功能，需要实现传入xlsx的接口，对应数据库表（不传id，用name代替，然后插入数据库用联表去查id）

### 分页注意

1. 模块实现批量删除的功能，注意：这是硬删除
2. 所有列表右边都有两个按钮：1.删除（硬删除） 2. 详情（点击可以查看所有和修改）

### 任务1【完成数据表】
根据db\migration\table\DataTabl11.md的数据库表，创建并完善V8__certificate__competition.sql,放到flyway的迁移下面，要注意索引，注释等规范

### 任务2【完成本科专业-考研方向关联表 (t_major_postgrad_direction)】

1. 列表展示：major_name,postgrad_major_name，created_at右边有详情，删除，
2. 点击详情，展示关联表所有字段
3. 模糊查询：major_name，postgrad_major_name


### 任务5【完成接口和功能概述文档】
创建接口和功能概述文档，命名为：order11.md,放到：Project-HaiFeng\Products，这个下面

### 功能统一需求

1. 统一用雪花算法
2. 按子模块独立Controller，每个子模块独立Controller，复用现有Excel导入模式

### 总结
1. 简单的增删改查，分页展示

### XLSX注意事项：

1. 一个是主表，其他的都是外键，有一对多的，有1对1的，1对1的不能使得外键或者能代替外键的重复，
2. 有外键的xlsx都不会传外键id，而是能代替外键唯一的字段，比如大学名，城市名，需要联表找出id并传入（先检查name对应的外键是否存在）
3. 关于如何识别哪一列跟数据库相对的问题，我认为处理方案是按名称，使用 EasyExcel 的 @ExcelProperty("院校名称") 注解即可，针对这次的名称，结合注释等信息自己决定就可以，后续列出来即可。
4. text[]字段的值必须用逗号格式，支持中英文逗号分隔，意思是逗号分隔，代表是不同的值
````
可以写一个自定义的 Converter：

Java
// 伪代码示例
public class StringToArrayConverter implements Converter<String[]> {
@Override
public String[] convertToJavaData(ReadCellData<?> cellData, ...) {
String val = cellData.getStringValue();
if (val == null) return null;
// 用正则同时兼容中英文逗号
return val.split("[,，]");
}
}
然后在 DTO 字段上加上 @ExcelProperty(converter = StringToArrayConverter.class) 即可。
````
5. 可能涉及到类型转换的问题：基础类型（如 Excel 里的 "2026-05-01" 转 Date，"1000" 转 Integer），EasyExcel 和 Spring Boot 默认就能完美转换。
6. 上传过程中遇到了错误，报第几行的具体错误，比如类型错误呀什么的
7. 使用事务保证一致性
8. 如果内容遇到了空值就默认是null，让数据库判断去吧。意思就是除了数据库指定不能为空之外，其他都可以为空
9. 正确顺序：传入的数据表，根据接口不同，有的是主表，有的不是主表
- 主表：检查表头规范性（比如检查看是否有超过规定的表头），检查unique字段的这一列是否有重复的,正确的话就传入数据
- 外键表：先检查院校名（1对1的不能重复，外键的必须在主表里面有对应的）不能重复检查外键表是否有数据，没有数据就报错，有数据就继续
- 错误处理：明确说清楚是什么报错了，比如哪一行，哪个表头，要明确信息
- 最佳实践：这就需要用到 Spring 的事务一致性。
````
在解析 Excel 的过程中，如果有校验不通过的（比如“第5行：院校名称已存在”），把这些错误信息收集到一个 List<String> errorMsgs 里。
如果 errorMsgs 不为空，直接抛出一个自定义异常 throw new BusinessException(String.join(";", errorMsgs))，并中断解析。
只要你的 Service 方法上加了 @Transactional(rollbackFor = Exception.class)，抛出异常后，前面插入的几行数据会自动全部回滚。
````
10. JSONB需要在原表的的基础上新建sheet1，sheet2等：Sheet 0：基础主表 (必须存在)，Sheet 1：附属labs_jsonb 字段（需要有逻辑外键）
11. xlsx的传入功能之前已实现过，可以借鉴

